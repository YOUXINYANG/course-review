# 计算机网络

> 建议按“分层—寻址—转发—可靠传输—应用协议”串联回答，避免只背协议名称。

## 目录

- [一、网络体系结构](#一网络体系结构)
- [二、数据链路与局域网](#二数据链路与局域网)
- [三、网络层](#三网络层)
- [四、传输层](#四传输层)
- [五、应用层](#五应用层)
- [常见面试提问](#常见面试提问)
---

## 一、网络体系结构

五层模型依次为物理层、数据链路层、网络层、传输层和应用层。下层向上层提供服务：物理层传比特，链路层成帧并完成同一链路传输，网络层负责跨网路由，传输层完成进程到进程通信，应用层提供具体服务。

## 二、数据链路与局域网

链路层核心问题是成帧、差错检测、可靠传输和介质访问控制。共享式有线以太网使用 CSMA/CD；无线局域网使用 CSMA/CA、ACK 和 RTS/CTS 降低冲突。交换机按 MAC 表转发帧；VLAN 在二层划分逻辑广播域。

## 三、网络层

IP 提供无连接、尽力交付的数据报服务；路由器按最长前缀匹配选择下一跳。ARP 将 IPv4 地址解析为 MAC，DHCP 动态配置地址，ICMP 用于差错报告和诊断。RIP、OSPF 和 BGP 分别服务于不同路由场景。

## 四、传输层

TCP 面向连接并通过序号、确认、重传、窗口和拥塞控制提供可靠字节流；UDP 无连接、面向报文、首部小。流量控制保护接收端，拥塞控制保护网络。

## 五、应用层

DNS 负责域名解析；HTTP 提供 Web 资源传输；FTP 传输文件；SMTP 投递邮件，POP3/IMAP 供客户端取信或同步。

---

## 常见面试提问

#### 一、基础与分层

#### 1. 计算机网络由什么构成？

硬件包括主机、通信链路、交换设备和通信处理设备；软件包括网络操作系统和应用；协议规定通信规则。网络的目标是资源共享和信息交换。

#### 2. 常见分类？

按范围可分 PAN、LAN、MAN、WAN；按传输技术可分广播式网络与点对点网络；按交换技术有电路交换、报文交换和分组交换。互联网主要采用分组交换的存储转发方式。

#### 3. 五层模型和各层职责？

物理层传比特；数据链路层成帧、介质访问、差错检测；网络层负责逻辑寻址与路由转发；传输层提供端到端进程通信；应用层提供具体网络服务。OSI 七层在五层模型上进一步拆出会话层和表示层。

#### 4. 协议的三个要素？

**语法**规定报文格式和编码，**语义**规定字段含义及控制动作，**同步**规定事件先后和时序。

#### 5. 点对点通信与端到端通信？

点对点是相邻结点间的单跳通信，主要由物理层和链路层协议保障；端到端是源主机进程到目的主机进程的通信，传输层负责其可靠性、复用和分用。

---

### 二、物理层与数据链路层

#### 1. 信号、码元、波特和比特？

信号是数据在介质上的表现；码元是在固定时长内代表一个或多个比特的基本波形。波特率是每秒码元数，比特率是每秒传输的比特数；若一个码元表示 `k` 比特，则比特率约为波特率乘 `k`。

#### 2. 网络时延包含哪些部分？

发送时延（把分组推上链路，约为分组长度/链路速率）、传播时延（信号在介质中传播，约为距离/传播速度）、处理时延和排队时延。高负载时排队时延常最不稳定。

#### 3. 常见编码和调制？

数字基带编码有 NRZ、曼彻斯特和差分曼彻斯特等；数字数据通过调幅、调频、调相（或组合）可调制为模拟信号。模拟数据数字化的典型步骤是采样、量化和编码。

#### 4. 数据链路层如何成帧和差错控制？

成帧可用字符计数、字节填充、比特填充或违规编码。差错检测常用奇偶校验和 CRC；海明码可纠正一位错。可靠传输还需确认、序号、超时重传和滑动窗口等机制。

#### 5. CSMA/CD 和 CSMA/CA 的区别？

CSMA/CD 用于传统共享式有线以太网：先监听、边发边检测冲突，冲突后停止并二进制指数退避。无线端难以边发边检测，802.11 使用 CSMA/CA，通过随机退避、ACK、RTS/CTS 尽量避免冲突；“避免”不等于绝不冲突。

#### 6. 二进制指数退避？

第 `i` 次重传从 `0` 到 `2^k-1` 个争用期随机选等待时间，`k=min(i,10)`；重传次数达到协议上限（以太网经典值为 16）则放弃帧并上报。其目的是在冲突多时扩大随机窗口。

#### 7. VLAN 是什么？标签何时出现？

VLAN 在二层把交换机端口按逻辑广播域划分，隔离广播且不受物理位置限制。接入端口通常收发无标签帧并归入配置 VLAN；中继（trunk）链路通常携带 802.1Q 标签以标识 VLAN，出口接入端口再去标签。

#### 8. 交换机的直通、存储转发和碎片隔离？

直通交换读到目的 MAC 就转发，延迟低但可能转发错误帧；存储转发收完整帧并校验 FCS，可靠性高；碎片隔离先读前 64 字节再转发，用于过滤碰撞碎片，是两者折中。

---

### 三、网络层

#### 1. IPv4 数据报和分片？

IPv4 数据报由首部和数据组成，基本首部 20 字节。若报文长度超过下一跳 MTU，IPv4 可在源主机或路由器处分片；目的主机按标识、片偏移和 MF 标志重组。分片会增大丢失代价，应尽量避免。

#### 2. IPv4 与 IPv6 的主要区别？

IPv4 地址 32 位，IPv6 地址 128 位；IPv6 基本首部固定 40 字节、字段更简化、取消首部校验和，扩展功能放入扩展首部。IPv6 路由器不分片，需由源主机借助路径 MTU 发现处理。过渡方式包括双栈和隧道。

#### 3. 子网掩码如何确定网络号和主机数？

IP 地址与子网掩码逐位与得到网络号。若主机位有 `h` 位，普通子网可用主机数通常为 `2^h - 2`（网络地址和定向广播地址不可分配）；特殊点到点链路有例外。

#### 4. MAC、IP 和端口分别标识什么？

MAC 标识同一链路内的网络接口；IP 标识网络层的主机/接口并用于跨网路由；端口标识主机上的传输层通信端点（通常对应进程服务）。一段通信可由五元组唯一标识。

#### 5. ARP、DHCP 和 ICMP？

ARP 在局域网内把已知 IPv4 地址解析为 MAC 地址，请求广播、应答通常单播。DHCP 基于 UDP 动态配置地址、掩码、网关和 DNS，经典过程为 Discover、Offer、Request、ACK。ICMP 用于差错报告和网络诊断，`ping` 使用 ICMP 回显请求/应答。

#### 6. 路由器如何转发分组？

路由器查看目的 IP，对路由表做**最长前缀匹配**，确定出接口和下一跳；若下一跳在本链路，再通过 ARP/邻居发现得到链路层地址并封装新帧。它不会把原始二层帧原样跨网转发。

#### 7. RIP、OSPF、BGP 的区别？

RIP 是距离向量协议，以跳数为度量、最大 15 跳、收敛较慢；OSPF 是自治系统内部的链路状态协议，洪泛链路状态并用 Dijkstra 计算最短路；BGP 用于自治系统之间，基于路径属性并重视策略与可达性。

#### 8. NAT 有什么作用和模式？

NAT 在边界把私有地址与公网地址转换，缓解 IPv4 地址不足并隐藏内部编址。静态 NAT 一对一，动态 NAT 从地址池映射，多对一的 PAT/NAPT 还通过端口号区分会话。NAT 会破坏端到端可达性，某些协议需额外穿透机制。

---

### 四、传输层

#### 1. TCP 与 UDP 的区别？

TCP 面向连接、可靠、字节流，提供确认重传、流量控制和拥塞控制；UDP 无连接、面向报文、尽力交付，首部小、时延低。实时音视频、DNS 等常用 UDP，文件传输和网页传输常用 TCP 或基于 UDP 构建可靠协议。

#### 2. TCP 如何保证可靠传输？

使用校验和检测差错、序号保证有序和去重、累计确认确认已收数据、超时重传处理丢失、三次重复 ACK 触发快速重传，并以滑动窗口进行流量控制。可靠不等于永不丢包，而是对上层屏蔽或报告丢失。

#### 3. 为什么 TCP 三次握手？

客户端发送 `SYN`，服务端回复 `SYN+ACK`，客户端回复 `ACK`。双方由此确认彼此的发送与接收能力，并同步初始序号。两次握手无法让客户端确认服务端收到了自己的确认，也难以防止历史重复连接请求造成错误连接。

#### 4. TCP 为什么通常四次挥手？

一方发送 `FIN` 表示本方向无数据，另一方先 `ACK`；待其剩余数据发完后再发送自己的 `FIN`，最后收到确认。收发方向可独立关闭，因此确认和结束常不能合并。主动关闭方的 `TIME_WAIT` 用于可靠完成末次确认并让旧报文消失。

#### 5. 流量控制和拥塞控制区别？

流量控制是端到端的，接收方通过接收窗口防止自身缓冲溢出；拥塞控制面向网络，发送方通过拥塞窗口避免向链路和路由器注入过多数据。实际发送窗口取两者允许范围的较小值。

#### 6. TCP 拥塞控制的基本思想？

慢开始阶段拥塞窗口随 RTT 快速增长，到达门限后进入拥塞避免的线性增长；发生超时时通常大幅降低窗口，三次重复 ACK 可快速重传并进入较温和的快速恢复。目标是在带宽利用率和网络稳定性之间平衡。

---

### 五、应用层与网络设备

#### 1. DNS 的递归和迭代查询？

递归查询要求被询问的服务器代为获得最终答案；迭代查询中服务器返回自己知道的最佳下一步线索，由查询者继续询问。客户端通常向本地递归解析器查询，解析器再迭代访问根、顶级域和权威服务器，并缓存结果。

#### 2. HTTP、FTP、SMTP、POP3？

HTTP 是无状态的应用层协议，常运行在 TCP（HTTPS 为 TLS over TCP）之上，可用持久连接；FTP 用控制连接和数据连接传文件；SMTP 负责邮件投递，POP3/IMAP 用于客户端取信或同步。HTTP 本身不等于“无连接”，其连接复用由版本和实现决定。

#### 3. 中继器、集线器、交换机和路由器区别？

中继器、集线器工作在物理层，前者再生信号，后者是多端口中继器并向各端口转发；交换机工作在数据链路层，按 MAC 表转发帧；路由器工作在网络层，按 IP 路由表转发分组并隔离广播域。

#### 4. 默认网关是什么？

主机要发送到非本地子网时，将 IP 数据报交给配置的默认网关（通常是本地网段路由器接口）。默认网关不是任意“网络端口”，而是本地主机已知的下一跳路由器地址。

#### 5. C/S、B/S 与 P2P？

C/S 将客户端和服务器职责明确分开；B/S 是以浏览器为客户端的 C/S 形式，部署方便；P2P 中结点既可请求服务也可提供服务，具备扩展性但管理、可用性和安全性更复杂。
