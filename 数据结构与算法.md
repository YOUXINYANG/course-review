# 数据结构与算法

> 复试回答不仅要说定义，还要交代适用场景、复杂度和边界条件。

## 目录

- [一、线性表](#一线性表)
- [二、树与二叉树](#二树与二叉树)
- [三、图](#三图)
- [四、查找与排序](#四查找与排序)
- [常见面试提问](#常见面试提问)
---

## 一、线性表

顺序表物理连续，随机访问为 `O(1)`，插删通常为 `O(n)`；链表通过指针连接，已知位置插删为 `O(1)`，按位访问为 `O(n)`。栈后进先出，适合递归和表达式；队列先进先出，适合 BFS 和缓冲；循环队列以取模复用空间。

## 二、树与二叉树

完全二叉树适合顺序存储；堆是满足堆序性质的完全二叉树。BST 操作复杂度为 `O(h)`，AVL 通过旋转维持平衡。哈夫曼树通过合并最小权值结点构造，得到最优前缀编码。

## 三、图

邻接矩阵空间 `O(V^2)`，适合稠密图；邻接表空间 `O(V+E)`，适合稀疏图。BFS/DFS 用于遍历和连通性；Prim、Kruskal 求最小生成树，Dijkstra 和 Floyd 求最短路径。

## 四、查找与排序

哈希表通过哈希函数定位，冲突可用链地址法或开放定址法解决；装填因子影响性能。求数组最大最小值可一次扫描 `O(n)`，成对比较可减少比较次数。

---

## 常见面试提问

#### 一、线性表、栈与队列

#### 1. 数据结构的三要素是什么？

数据的**逻辑结构**描述元素间关系，**存储结构**描述其在计算机中的表示，**数据运算**描述可执行的操作。相同逻辑结构可有不同存储结构，例如线性表可用数组或链表实现。

#### 2. 顺序表和链表的优缺点？

顺序表元素物理连续，支持 `O(1)` 随机访问、存储密度高；中间插删需移动元素，最坏 `O(n)`。链表插删已知位置时仅改指针 `O(1)`，但访问第 `i` 个元素需顺序遍历 `O(n)`，并有指针开销和较差缓存局部性。

#### 3. 为什么链表常设置头结点？

空表和非空表的头指针都非空，且首结点插删可与一般位置统一处理，减少边界分支。注意头结点通常不保存有效数据。

#### 4. 如何原地逆置单链表？

维护 `prev`、`cur`、`next` 三个指针：每次先保存 `cur->next`，再令 `cur->next = prev`，随后推进 `prev` 和 `cur`。遍历一次，时间 `O(n)`、额外空间 `O(1)`。

#### 5. 如何合并两个有序链表？

用两个游标比较当前结点，将较小者接到结果尾部；一条链表耗尽后接上另一条剩余部分。时间 `O(m+n)`，仅需常数辅助空间；若要求稳定，相等时先取第一条链表。

#### 6. 栈、队列和堆的区别？

栈是后进先出，只在栈顶操作，常用于括号匹配、递归和表达式求值。队列是先进先出，在队尾入队、队头出队，常用于 BFS 和缓冲。堆是满足堆序性质的完全二叉树：大根堆根最大、小根堆根最小，适合优先队列。

#### 7. 循环队列为什么能解决“假溢出”？

顺序队列出队后前部空位无法复用；循环队列让下标按模长回绕。常见“牺牲一个单元”方案中：空为 `front == rear`，满为 `(rear + 1) % capacity == front`。

#### 8. 双端队列和受限双端队列？

双端队列两端都可入队、出队。受限双端队列限制其中一端只能入或只能出；它常用于判断给定出入序列能否实现等问题。

#### 9. 稀疏矩阵如何压缩？

当非零元素远少于总元素时，只存非零项及其行列坐标和值，例如三元组表、十字链表。普通二维数组按行优先或列优先映射到一维；对称矩阵、三角矩阵可只存一半。

---

### 二、树与二叉树

#### 1. 满二叉树和完全二叉树？

满二叉树每层结点数都达到最大；高度为 `h`（根为第 1 层）时有 `2^h - 1` 个结点。完全二叉树除最后一层外均满，最后一层从左到右连续填充，适合用数组存储。

#### 2. 仅知先序和后序遍历能唯一确定二叉树吗？

一般**不能**，因为只有一个孩子的结点无法判断孩子在左还是右。先序加中序、后序加中序通常可唯一确定元素互异的二叉树；层序加中序也可以。

#### 3. 树有哪些存储和遍历方式？

树可用双亲表示法、孩子表示法或孩子兄弟表示法。孩子兄弟表示法中左指针指向第一个孩子、右指针指向下一个兄弟，可把一般树转换为二叉树。树常用先根/后根遍历；二叉树有先、中、后、层序遍历。

#### 4. 什么是线索二叉树？

把原本为空的左右指针改作指向遍历序列前驱或后继的线索，并以标志位区分线索和孩子指针。它能在不使用栈或递归的情况下按指定次序遍历；前驱/后继的定义必须说明是先序、中序还是后序线索。

#### 5. 二叉排序树（BST）的性质和查找？

对每个结点，左子树键值小于结点、右子树键值大于结点，子树也满足该性质。查找时与根比较后向左或向右继续，时间为树高 `O(h)`；极端退化时为 `O(n)`。

#### 6. 平衡二叉树如何处理插入失衡？

AVL 树任一结点左右子树高度差绝对值不超过 1。插入导致失衡时按路径判定：LL 做右旋，RR 做左旋，LR 先左旋后右旋，RL 先右旋后左旋。

#### 7. 哈夫曼树和哈夫曼编码？

哈夫曼树是带权路径长度最小的二叉树：反复合并权值最小的两棵树。它无度为 1 的结点，形态可能不唯一。左、右边分别编码为 `0/1` 后得到前缀码，平均编码长度最短。

---

### 三、图

#### 1. 邻接矩阵与邻接表如何选择？

邻接矩阵空间 `O(V^2)`，判断两点是否有边为 `O(1)`，适合稠密图。邻接表空间 `O(V+E)`，遍历某顶点邻边为 `O(deg(v))`，适合稀疏图。BFS/DFS 用邻接表的总复杂度为 `O(V+E)`。

#### 2. 连通、连通分量和强连通分量？

无向图两点间有路径即连通；极大连通子图为连通分量。有向图任意两点可相互到达称强连通；极大强连通子图为强连通分量。

#### 3. 生成树、最小生成树与“极小连通子图”区别？

连通无向图的**生成树**包含全部顶点、无环且有 `V-1` 条边，是按边数极小的连通子图；**最小生成树**在所有生成树中边权和最小。不要把“极小”（删一条边就不连通）和“最小权值”混为一谈。

#### 4. Prim 和 Kruskal 的区别？

Prim 从一个顶点开始，反复选连接已选集合与未选集合的最小边，适合稠密图。Kruskal 把边按权排序，按从小到大选不成环的边，配合并查集，适合稀疏图。

#### 5. Dijkstra 与 Floyd 的适用条件？

Dijkstra 求单源最短路，要求边权非负；堆优化邻接表实现约为 `O((V+E)logV)`。Floyd 用动态规划求任意两点最短路，`O(V^3)`，可处理负边但不能有可达负环。

#### 6. 树和图的区别？

树是连通无环图，通常强调根和层次关系，任意两结点间路径唯一；图描述更一般的多对多关系，可有环、多个连通分量和任意方向边。

---

### 四、查找、哈希与算法题

#### 1. 什么是哈希表？如何解决冲突？

哈希函数将键映射到表地址，目标是平均 `O(1)` 查找。不同键映射到同一地址是冲突，常用**链地址法**、**开放定址法**（线性/二次探测、双重散列）或再散列处理。装填因子过高会显著降低性能。

#### 2. 常见构造哈希函数的方法？

除留余数法、直接定址法、数字分析法、平方取中法和折叠法。选择时要结合键分布与表长，避免规律键导致聚集。

#### 3. 很长数组如何高效求最大值和最小值？

一次线性扫描即可，时间 `O(n)`、空间 `O(1)`；若关注比较次数，可成对比较：每对先内部比较，再分别更新全局最小/最大，约 `3n/2` 次比较。排序或建堆都不是仅求两者时的最优选择。
