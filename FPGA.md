# FPGA

> 从《数电》拆分出的 FPGA 知识整理。

## 可编程逻辑器件概览

PLA、PAL、CPLD 和 FPGA 都可以通过编程实现数字逻辑。

- PLA 和 PAL 主要由可编程的与阵列/或阵列构成，适合实现规模相对有限的组合逻辑。
- CPLD 由多个逻辑块和可编程互连组成，适合中等规模、结构相对固定且需要较确定时序的逻辑。
- FPGA 通常由查找表（LUT）、触发器、可编程互连、块 RAM、DSP 和时钟资源组成，适合实现复杂的并行数字系统。

选择器件时需要综合考虑逻辑资源、存储资源、I/O 数量、时钟资源、功耗、开发工具和成本。

## FPGA 基础知识

FPGA（Field-Programmable Gate Array，现场可编程门阵列）是一种可以在出厂后通过配置数据实现不同数字电路的可编程器件。**FPGA 的核心特点是硬件结构可重构，并且能够利用大量并行逻辑同时处理多个数据通路。**

### 1. 基本组成

- **可配置逻辑块（CLB/逻辑单元）**：通常包含 LUT、触发器、进位链和局部多路选择器，是 FPGA 实现组合逻辑和时序逻辑的基本单位。
- **查找表（LUT）**：本质上是一个小容量存储器，通过配置内容实现任意多输入组合逻辑。
- **触发器（FF）**：用于保存状态、构成寄存器和流水线。LUT 与 FF 配合可以实现同步时序电路。
- **可编程互连资源**：通过开关矩阵和布线通道连接不同逻辑块、存储器、DSP 和 I/O。
- **块 RAM（Block RAM）**：用于实现片上存储器、FIFO、查找表和缓存。
- **DSP 资源**：通常包含乘法器、加法器和累加器，适合信号处理和矩阵运算。
- **I/O 资源**：负责 FPGA 内部逻辑与外部引脚之间的连接，可配置输入输出标准、驱动能力和边沿速度。
- **时钟资源**：包括 PLL、MMCM、全局时钟网络等，用于时钟倍频、分频、相位调整和低偏斜分发。

### 2. FPGA 实现逻辑的方式

组合逻辑通常映射到 LUT，寄存器和状态机映射到触发器，乘加运算优先使用专用 DSP，较大容量的数据存储使用块 RAM。综合工具会根据 HDL 代码和约束完成逻辑综合、优化、布局布线，最后生成用于配置 FPGA 的比特流文件（bitstream）。

### 3. FPGA 的典型开发流程

1. 使用 Verilog、SystemVerilog 或 VHDL 描述硬件功能。
2. 编写仿真测试平台，验证组合逻辑、状态机和边界条件。
3. 进行综合，将 RTL 描述转换为门级逻辑和 FPGA 资源。
4. 进行布局布线，并根据时序约束检查建立时间、保持时间和时钟偏斜。
5. 生成并下载 bitstream，结合板级信号进行调试和验证。

### 4. 前仿与后仿

前仿（前综合仿真、RTL 仿真）直接以 RTL 代码为仿真对象，主要验证功能逻辑是否正确。它通常采用理想化的零延迟模型，仿真速度快，适合在设计早期发现功能、状态机和边界条件问题。

后仿（后综合或后布局布线仿真）以综合网表或布局布线后的网表为仿真对象。后布局布线仿真可结合 SDF（Standard Delay Format）反标的单元与连线延迟，因而能够暴露部分时序相关问题，但仿真速度较慢。

| 对比项 | 前仿 | 后仿 |
| --- | --- | --- |
| 仿真对象 | RTL 代码 | 综合网表或布局布线网表 |
| 主要目标 | 验证功能正确性 | 验证网表一致性和时序相关行为 |
| 时延模型 | 通常为零延迟或理想延迟 | 可包含单元延迟、布线延迟和时序检查 |
| 仿真速度 | 较快 | 较慢 |
| 常见用途 | 开发早期的功能验证 | 时序敏感接口、门控/复位等问题定位 |

**注意：** 后仿不能替代静态时序分析（STA）。FPGA 设计的时序收敛主要仍应依据正确的时序约束和 STA 报告；后仿更适合补充验证具体时序场景与网表行为。

### 5. FPGA 设计中的关键原则

- **优先采用同步设计**：使用明确的时钟和寄存器边界，避免随意使用组合反馈。
- **正确编写时序约束**：声明时钟周期、输入输出延迟和异步时钟关系，否则工具无法准确分析时序。
- **处理跨时钟域信号**：单 bit 信号使用同步器，多 bit 数据使用握手协议或异步 FIFO。
- **合理使用复位**：明确同步/异步复位策略，复位释放应避免造成亚稳态或状态不一致。
- **利用流水线提高频率**：当组合逻辑路径过长时插入寄存器，缩短关键路径，但会增加延迟和资源消耗。
- **关注资源与功耗**：避免不必要的高扇出、过多组合翻转和低效的存储器/乘法器实现。

### 6. FPGA 与 CPU、ASIC 的区别

FPGA 具有硬件并行性和可重构性，适合原型验证、算法加速、通信、图像处理和实时控制；CPU 编程灵活、软件生态完善，但并行硬件资源有限；ASIC 性能、功耗和单位成本通常更优，但一次流片后功能难以修改、开发周期和前期成本较高。

## Verilog 与 SystemVerilog 基础

### 1. 模块、端口与常用数据类型

Verilog/SystemVerilog 通过 `module` 描述硬件模块，使用 `input`、`output` 和 `inout` 声明端口。端口和内部信号应显式声明位宽，例如 `logic [7:0] data` 表示 8 位向量。

- **Verilog 的 `wire`**：表示由连续赋值或模块输出驱动的连线（net）。
- **Verilog 的 `reg`**：表示可在 `always`/`initial` 过程块中赋值的变量；它不一定对应物理寄存器，是否综合为触发器取决于编码方式。
- **SystemVerilog 的 `logic`**：可在大多数 RTL 场景中替代 `reg`，既可用于组合逻辑，也可用于时序逻辑；对于需要多驱动解析的连线仍应使用 net 类型。
- **位宽与有符号性**：应明确声明 `[MSB:LSB]` 和 `signed`/`unsigned`，避免表达式自动扩展、截断或符号扩展导致的错误。

### 2. 连续赋值与过程块

`assign` 用于描述持续生效的组合逻辑；`always` 块用于描述组合或时序行为。一个信号应避免被多个过程块或连续赋值同时驱动。

```systemverilog
assign y = a & b;              // 连续赋值：组合逻辑

always_comb begin              // 组合逻辑
    y = sel ? a : b;
end

always_ff @(posedge clk) begin // 时序逻辑
    q <= d;
end
```

### 3. 阻塞赋值与非阻塞赋值

- **阻塞赋值 `=`**：语句按顺序立即执行，通常用于组合逻辑过程块。
- **非阻塞赋值 `<=`**：在当前时间步结束时统一更新左值，符合多个触发器在同一时钟沿同时采样的硬件行为，通常用于时序逻辑过程块。

组合逻辑中应为每个输出在所有分支赋值；时序逻辑中应使用统一时钟沿，并根据需求明确复位策略。不要在同一时序过程块中混用 `=` 与 `<=` 来描述寄存器状态更新。

### 4. 可综合 RTL 编码要点

- 使用 `always_comb`、`always_ff`（或规范的 `always @(*)`、`always @(posedge clk)`）明确硬件意图。
- 组合逻辑为所有输出提供默认值或覆盖全部分支，防止综合出锁存器。
- `case` 语句应覆盖所有可能情况，通常保留 `default` 分支；对 `unique case`、`priority case` 应确保其语义与实际硬件需求一致。
- 避免使用不可综合或依赖仿真时间的语句，如任意 `#delay`、大多数 `initial` 行为和动态数据结构；具体可综合范围以目标 FPGA 工具为准。
- 避免隐式网线：建议使用 `` `default_nettype none ``，使拼写错误在编译阶段暴露。

### 5. SystemVerilog 常用增强

SystemVerilog 在 Verilog 基础上增强了 RTL 表达能力。

- `always_comb` 自动包含组合逻辑敏感信号，并检查常见的多驱动/遗漏赋值问题。
- `always_ff` 明确寄存器过程块，便于工具检查时钟和赋值规则。
- `typedef enum logic [1:0]` 可为状态机定义具名状态，提升可读性与类型检查能力。
- `struct`、`interface`、`package` 可组织复杂接口和公共定义；其中 class、随机化与断言等特性更多用于验证环境，是否可综合需视具体语法和工具而定。

## 面试可能提问

### 一、基础概念

#### 1. FPGA 与 CPLD、CPU、ASIC 分别有什么区别？

**答题要点：** FPGA 基于 LUT、触发器和可编程互连，具有硬件级并行和可重构性；CPLD 规模较小、时序更确定；CPU 依靠顺序执行指令，软件开发灵活；ASIC 在性能、功耗和单位成本方面通常更优，但流片后难以修改。

#### 2. LUT 是什么？为什么它能够实现任意组合逻辑？

**答题要点：** LUT 本质是存储真值表的小型 RAM。对于有 $n$ 个输入的组合逻辑，只需将 $2^n$ 种输入组合对应的输出预先写入 LUT，即可实现该逻辑函数。

#### 3. FPGA 中一个逻辑单元通常包含哪些资源？

**答题要点：** 通常包括 LUT、触发器、进位链和局部多路选择器；不同系列还会集成分布式 RAM、移位寄存器等功能。LUT 负责组合逻辑，触发器负责保存状态。

#### 4. BRAM、分布式 RAM 和寄存器阵列如何选择？

**答题要点：** 大容量、规则的存储优先使用 BRAM；小容量、靠近逻辑单元的查找表或缓存可使用分布式 RAM；很小且需要最高访问灵活性的存储可由寄存器实现，但资源消耗较大。

#### 5. 什么是 bitstream？FPGA 上电后为什么需要配置？

**答题要点：** bitstream 是描述 LUT 内容、互连开关、I/O 和时钟资源配置的数据。多数 FPGA 的配置存储为易失性，上电后需要从外部 Flash、处理器或下载器重新加载配置数据。

### 二、设计流程与时序

#### 6. FPGA 的典型开发流程是什么？

**答题要点：** 使用 HDL 编写 RTL 和测试平台，进行功能仿真；随后完成综合、约束、布局布线和静态时序分析；最后生成 bitstream、下载到开发板并进行板级调试。

#### 7. 综合与布局布线分别做什么？

**答题要点：** 综合将 RTL 描述映射、优化为 LUT、触发器、BRAM 和 DSP 等目标器件资源；布局布线则决定各资源在芯片中的物理位置和连线，直接影响时序、拥塞和功耗。

#### 8. 什么是前仿和后仿？两者的主要区别是什么？

**答题要点：** 前仿直接仿真 RTL，侧重功能正确性，通常不包含真实的物理时延；后仿仿真综合后或布局布线后的网表，可结合 SDF 反标延迟，侧重检查网表一致性和时序相关行为。前仿速度更快，后仿更接近实现结果但速度较慢。

#### 9. 后仿中的时延信息来自哪里？

**答题要点：** 后综合仿真可使用工艺库单元延迟；后布局布线仿真的延迟主要来自布局布线结果，包括逻辑单元延迟与实际布线延迟，通常以 SDF 文件反标到仿真网表中。具体支持方式取决于器件和工具链。

#### 10. 为什么前仿通过、后仿或上板仍可能失败？

**答题要点：** 前仿通常未体现真实时钟、复位释放、布线时延、I/O 时序和 CDC 问题。后仿或上板失败还可能源于时序约束缺失、初始化差异、IP 模型不一致、信号完整性或板级连接问题；应结合 STA 报告与板级调试定位。

#### 11. 为什么必须编写时序约束？常见约束有哪些？

**答题要点：** 约束为工具提供时钟和外部接口的时序关系，否则静态时序分析没有可靠依据。常见约束包括主时钟周期、生成时钟、输入/输出延迟、假路径和多周期路径。

#### 12. 什么是建立时间、保持时间和关键路径？

**答题要点：** 建立时间要求数据在采样时钟沿之前稳定，保持时间要求在时钟沿之后继续稳定一段时间。关键路径是时延最大的寄存器到寄存器路径，决定可达到的最高工作频率。

#### 13. 时序不满足时有哪些常用优化方法？

**答题要点：** 首先确认约束和时钟定义正确；再通过插入流水线寄存器、优化组合逻辑、使用 DSP/BRAM 硬核、降低高扇出和改善布局布线来缩短关键路径。不能仅靠放宽约束掩盖问题。

#### 14. 什么是跨时钟域（CDC）？如何处理单 bit 和多 bit 信号？

**答题要点：** CDC 指信号在异步或不确定相位关系的时钟域之间传递，可能导致亚稳态。单 bit 控制信号常用两级同步器；多 bit 数据应使用握手协议、异步 FIFO 或带有效标志的数据通路，不能简单逐位同步。

### 三、工程实践

#### 15. 为什么不建议直接用逻辑门生成时钟？

**答题要点：** 组合逻辑会产生毛刺和不可控延迟，导致时钟偏斜或误触发。应使用专用时钟资源，如 PLL/MMCM、时钟使能或器件提供的时钟门控单元。

#### 16. 同步复位和异步复位如何选择？

**答题要点：** 同步复位只在时钟沿生效，便于时序收敛；异步复位能在无时钟时立即复位，但释放时必须同步化，避免亚稳态和不同寄存器释放不一致。选择需结合芯片、系统启动和时序要求。

#### 17. 仿真通过后，为什么上板仍可能失败？

**答题要点：** 仿真可能未覆盖真实时钟、复位、I/O 电平、初始化和时序问题；上板还会受到约束错误、CDC、信号完整性和外设协议的影响。可通过静态时序分析、片上逻辑分析仪（ILA）和逐级定位缩小问题范围。

#### 18. 如何降低 FPGA 的资源占用和功耗？

**答题要点：** 复用硬件资源，优先推断 BRAM 和 DSP，避免无效位宽、过多寄存器和不必要的高扇出；对不工作的模块使用时钟使能降低翻转活动，并在满足时序的前提下优化频率和流水线深度。

### 四、Verilog 与 SystemVerilog

#### 19. Verilog 中 `wire` 和 `reg` 有什么区别？`reg` 一定会综合成寄存器吗？

**答题要点：** `wire` 是 net 类型，通常由 `assign` 或模块端口驱动；`reg` 是可在过程块中赋值的变量。`reg` 仅表示过程赋值的对象，不等于物理寄存器：在组合逻辑块中完整赋值时可综合为组合逻辑，在时钟触发块中赋值时才通常综合为触发器。

#### 20. 阻塞赋值 `=` 与非阻塞赋值 `<=` 应如何使用？

**答题要点：** 组合逻辑通常使用阻塞赋值，以表达语句间的计算顺序；时序逻辑通常使用非阻塞赋值，以模拟触发器在同一时钟沿同时更新。若时序逻辑错误使用阻塞赋值，可能造成仿真与实际硬件理解不一致。

#### 21. 什么情况下会综合出锁存器？如何避免？

**答题要点：** 在组合逻辑过程块中，若某个输出在部分条件或 `case` 分支中未被赋值，综合器会为保持其旧值推断锁存器。应给输出设置默认值，或保证 `if-else` 与 `case` 覆盖所有路径，并优先使用 `always_comb`。

#### 22. SystemVerilog 相比 Verilog 有哪些常用改进？

**答题要点：** RTL 设计中最常用的是 `logic`、`always_comb`、`always_ff`、`enum`、`struct`、`interface` 和 `package`。它们加强了类型表达、模块接口组织和工具静态检查；SystemVerilog 的 class、随机化和约束等能力则主要服务于验证。
