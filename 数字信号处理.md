# 数字信号处理

> 用于保研笔试、面试与课程复习。以离散时间信号与系统为基础，重点掌握频谱分析、DFT/FFT、数字滤波器设计与实现。

## 目录

- [一、离散时间信号与系统](#一离散时间信号与系统)
- [二、采样、量化与重构](#二采样量化与重构)
- [三、DTFT 与 Z 变换](#三dtft-与-z-变换)
- [四、DFT、FFT 与频谱分析](#四dftfft-与频谱分析)
- [五、数字滤波器基础](#五数字滤波器基础)
- [六、FIR 滤波器设计](#六fir-滤波器设计)
- [七、IIR 滤波器设计](#七iir-滤波器设计)
- [八、结构、有限字长与多速率处理](#八结构有限字长与多速率处理)
- [九、高频问答与易错点](#九高频问答与易错点)
- [附：重要公式汇总表](#附重要公式汇总表)

---

## 一、离散时间信号与系统

### 1. 基本序列与周期性

离散时间信号记为 $x[n]$，自变量 $n$ 为整数；其幅值可以连续，也可以经过量化后取离散值。数字信号通常是时间和幅值均离散的信号。

- **单位冲激序列**：$\delta[n]=1\;(n=0)$，其余时刻为 $0$，且 $x[n]\delta[n-n_0]=x[n_0]\delta[n-n_0]$。
- **单位阶跃序列**：$u[n]=1\;(n\geq0)$，$u[n]=0\;(n<0)$，并有 $\delta[n]=u[n]-u[n-1]$。
- **复指数序列**：$a^n$ 是离散 LTI 系统的特征函数；$a=e^{j\omega_0}$ 时得到复正弦序列。
- **周期性**：若存在最小正整数 $N$，使 $x[n+N]=x[n]$，则 $N$ 为基本周期。$e^{j\omega_0n}$ 有周期的条件是 $\omega_0/(2\pi)$ 为有理数。

离散时间频率以 rad/sample 表示。$\omega$ 与 $\omega+2\pi k$ 对应同一序列，因此离散时间频谱以 $2\pi$ 为周期。

### 2. 离散 LTI 系统与卷积

离散 LTI 系统由冲激响应 $h[n]$ 完全描述，零状态响应为：

$$
y[n]=x[n]*h[n]=\sum_{k=-\infty}^{\infty}x[k]h[n-k]
$$

若系统满足常系数差分方程：

$$
\sum_{k=0}^{N}a_k y[n-k]=\sum_{m=0}^{M}b_m x[n-m]
$$

则零初始条件下可借助 Z 变换求系统函数。因果系统要求 $h[n]=0\;(n<0)$；BIBO 稳定要求 $\sum_n|h[n]|<\infty$。

### 3. 相关与卷积

互相关可写为：

$$
r_{xy}[m]=\sum_{n=-\infty}^{\infty}x[n]y^*[n-m]
$$

相关衡量两个序列在相对延迟 $m$ 下的相似性，常用于同步、时延估计和匹配滤波；卷积用于计算 LTI 系统输出。两者形式相似，但相关包含共轭和时间反向，物理意义不同。

---

## 二、采样、量化与重构

### 1. 采样定理与混叠

设连续信号最高频率为 $f_m$，采样频率为 $f_s=1/T_s$。要由样点无失真恢复原信号，需满足：

$$
f_s\geq2f_m
$$

$2f_m$ 为奈奎斯特率，$f_s/2$ 为奈奎斯特频率。采样会使连续频谱以 $f_s$ 为间隔重复；若副本重叠，便发生**混叠**，后续数字处理通常无法完全消除。理想恢复时，让采样信号通过增益为 $T_s$、截止频率为 $f_s/2$ 的理想低通滤波器即可取出原频谱。

工程上应在 ADC 前使用**抗混叠低通滤波器**，将有效带宽限制在 $f_s/2$ 以下并预留过渡带。实际信号往往不是锐截止、实际滤波器也有过渡带，故 $f_s$ 通常取为高于奈奎斯特率的合适值；并非越高越好，因为它会增加数据量、运算时间和设备成本。

### 2. 量化与编码

量化将连续幅值映射到有限等级。若量化器有 $B$ 位，级数为 $L=2^B$；满量程为 $V_{\mathrm{FS}}$ 时，量化间隔近似为：

$$
\Delta=\frac{V_{\mathrm{FS}}}{2^B}
$$

均匀量化、不过载且量化误差可近似视为均匀白噪声时，误差功率为 $\Delta^2/12$。对满幅正弦信号，理想量化信噪比约为：

$$
\mathrm{SQNR}\approx6.02B+1.76\ \mathrm{dB}
$$

实际有效位数（ENOB）还会受噪声、非线性和时钟抖动影响。

### 3. D/A 重构

理想重构可理解为用低通滤波器去除采样后出现的高频频谱副本。实际 DAC 常有零阶保持引起的幅度下垂，因此输出端还需要重构/平滑滤波器。

---

## 三、DTFT 与 Z 变换

### 1. DTFT

离散时间傅里叶变换（DTFT）为：

$$
X(e^{j\omega})=\sum_{n=-\infty}^{\infty}x[n]e^{-j\omega n}
$$

其反变换为：

$$
x[n]=\frac1{2\pi}\int_{-\pi}^{\pi}X(e^{j\omega})e^{j\omega n}d\omega
$$

DTFT 的时域离散、频域连续，且频谱以 $2\pi$ 为周期。对 LTI 系统：

$$
Y(e^{j\omega})=H(e^{j\omega})X(e^{j\omega})
$$

其中 $|H(e^{j\omega})|$ 是幅频特性，$\angle H(e^{j\omega})$ 是相频特性。

### 2. 常用性质

- **时移**：$x[n-n_0]\leftrightarrow e^{-j\omega n_0}X(e^{j\omega})$。
- **调制**：$e^{j\omega_0n}x[n]\leftrightarrow X(e^{j(\omega-\omega_0)})$。
- **时域卷积**：$x[n]*h[n]\leftrightarrow X(e^{j\omega})H(e^{j\omega})$。
- **时域相乘**：对应频域的 $1/(2\pi)$ 倍周期卷积。
- **一阶差分**：$x[n]-x[n-1]\leftrightarrow(1-e^{-j\omega})X(e^{j\omega})$。

群延迟定义为：

$$
\tau_g(\omega)=-\frac{d\angle H(e^{j\omega})}{d\omega}
$$

线性相位意味着群延迟为常数，各频率分量延迟相同，波形不会发生相位失真。

### 3. Z 变换、ROC 与极零点

双边 Z 变换定义为：

$$
X(z)=\sum_{n=-\infty}^{\infty}x[n]z^{-n}
$$

Z 变换表达式与**收敛域**（ROC）共同唯一确定时域序列。

- 右边序列 ROC 位于最外极点之外；因果有理系统通常属于此类。
- 左边序列 ROC 位于最内极点之内。
- 稳定系统的 ROC 必须包含单位圆 $|z|=1$。
- 因果且稳定的有理系统，所有极点必须位于单位圆内。

当 ROC 包含单位圆，令 $z=e^{j\omega}$ 即得到 DTFT。零状态条件下的系统函数为：

$$
H(z)=\frac{Y(z)}{X(z)}=\frac{\sum_{m=0}^{M}b_mz^{-m}}{1+\sum_{k=1}^{N}a_kz^{-k}}
$$

极点越靠近单位圆，系统在相应频率附近的峰越尖、衰减越慢；极点在单位圆上时，因果系统一般不满足 BIBO 稳定。

### 4. 单边 Z 变换

单边（单侧）Z 变换只对 $n\geq0$ 求和：

$$
X^+(z)=\sum_{n=0}^{\infty}x[n]z^{-n}
$$

因此，两个序列只要在 $n\geq0$ 时完全相同，即使 $n<0$ 时不同，它们的单边 Z 变换也相同；对因果序列，单边和双边 Z 变换的结果一致。单边 Z 变换能自然保留初始条件，特别适合求差分方程的瞬态响应，可同时处理零输入响应和零状态响应。

### 5. 最小相位与全通系统

- **最小相位系统**：在稳定、因果前提下所有零点位于单位圆内；其逆系统也稳定因果，并在相同幅频特性下具有最小相位延迟。
- **全通系统**：单位圆上的幅度恒为常数，主要改变相位；实系数全通系统的零点与极点互为共轭倒数。

---

## 四、DFT、FFT 与频谱分析

### 1. DTFT、DFT 与 FFT 的定义

- **DTFT（离散时间傅里叶变换）**：将一般离散时间序列 $x[n]$ 映射为连续频率变量 $omega$ 的周期频谱 $X(e^{j\omega})$。它适合从理论上描述离散时间信号的频谱，时域离散、频域连续，频谱以 $2\pi$ 为周期。
- **DFT（离散傅里叶变换）**：将长度为 $N$ 的有限长序列映射为 $N$ 个离散频率样点 $X[k]$。可把它看作对 DTFT 在单位圆上等间隔取样；DFT 默认时域和频域均按 $N$ 周期延拓，适合计算机进行数值频谱分析。
- **FFT（快速傅里叶变换）**：快速计算 DFT 的一类算法，利用旋转因子的对称性和周期性消除重复运算。FFT 不产生新的变换结果，只是把直接计算 DFT 的效率提高。

三者关系可概括为：DTFT 给出连续频谱，DFT 给出有限长数据的离散频谱样点，FFT 则是求取这些 DFT 样点的高效方法。

### 2. DFT 与 IDFT

对长度为 $N$ 的序列，$N$ 点 DFT 为：

$$
X[k]=\sum_{n=0}^{N-1}x[n]e^{-j\frac{2\pi}{N}kn},\qquad k=0,1,\ldots,N-1
$$

反变换为：

$$
x[n]=\frac1N\sum_{k=0}^{N-1}X[k]e^{j\frac{2\pi}{N}kn}
$$

频率采样点为 $\omega_k=2\pi k/N$，频率分辨率为 $\Delta f=f_s/N$。DFT 隐含时域和频域均按 $N$ 周期延拓。

### 3. 循环卷积与线性卷积

DFT 中频域相乘对应时域的 $N$ 点**循环卷积**。若 $x[n]$ 长度为 $L_x$、$h[n]$ 长度为 $L_h$，将两者补零到：

$$
N\ge L_x+L_h-1
$$

再进行 DFT 相乘和 IDFT，所得一个周期就等于线性卷积。Parseval 定理为：

$$
\sum_{n=0}^{N-1}|x[n]|^2=\frac1N\sum_{k=0}^{N-1}|X[k]|^2
$$

周期卷积是线性卷积的周期延拓；圆周（循环）卷积是该周期序列的一个主值周期。只有当延拓周期满足 $N\geq L_x+L_h-1$、不存在时域混叠时，圆周卷积才等于线性卷积。

### 4. FFT

FFT 是快速计算 DFT 的算法，不是新的变换。直接 DFT 计算复杂度为 $O(N^2)$；典型基 2 FFT 利用偶、奇序列分解及旋转因子周期性，将复杂度降为 $O(N\log_2N)$。基 2 FFT 通常要求 $N=2^M$。

### 5. 二维 DFT 与二维 FFT

二维 DFT 用于分析二维离散信号（如图像）。对大小为 $M\times N$ 的序列 $x[m,n]$，二维 DFT 与反变换为：

$$
X[k,l]=\sum_{m=0}^{M-1}\sum_{n=0}^{N-1}x[m,n]e^{-j2\pi\left(\frac{km}{M}+\frac{ln}{N}\right)}
$$

$$
x[m,n]=\frac1{MN}\sum_{k=0}^{M-1}\sum_{l=0}^{N-1}X[k,l]e^{j2\pi\left(\frac{km}{M}+\frac{ln}{N}\right)}
$$

- **二维 FFT**：快速计算二维 DFT 的算法，而非新的变换。二维 DFT 具有可分离性：可先对每一行做 $N$ 点 FFT，再对所得结果的每一列做 $M$ 点 FFT；行、列计算的先后顺序不影响结果。
- **计算量**：直接二维 DFT 的复杂度约为 $O(M^2N^2)$；采用行列分解的二维 FFT 约为 $O(MN\log_2M+MN\log_2N)$。当 $M=N$ 时，约为 $O(N^2\log_2N)$。
- **频谱含义**：二维频谱的低频通常集中在原点附近，反映缓慢变化的背景；高频反映边缘、纹理和细节。实际显示时常把零频移到频谱中心（如对输入乘以 $(-1)^{m+n}$，或对结果作 `fftshift`），便于观察。
- **卷积性质**：二维空间域的循环卷积对应二维频域相乘。用二维 DFT 实现线性卷积时，两个维度都要补零：若数组大小分别为 $M_1\times N_1$ 与 $M_2\times N_2$，应取 $M\geq M_1+M_2-1$、$N\geq N_1+N_2-1$，以避免循环混叠。
- **应用**：二维 FFT 常用于图像频谱分析、图像平滑与锐化、周期噪声抑制、二维滤波和快速二维卷积。

### 6. 频谱泄漏、栅栏效应与加窗

有限长度截取等价于时域乘窗，因此频域为原频谱与窗频谱卷积。正弦频率未恰好落在 DFT 栅格上时，能量扩散到多个频点，称为**频谱泄漏**。

- 增大有效观测时长可减小主瓣宽度，提高真实频率分辨率。
- **零填充不提高真实分辨率**，只会加密频谱采样点，便于显示和插值。
- 汉宁、海明、布莱克曼等窗可降低旁瓣、减轻泄漏，但主瓣变宽，幅值标定也会变化。
- 栅栏效应指真实频率落在相邻 DFT 频点之间，峰值可能被低估；零填充与插值可改善估计。

泄漏来自有限长度截取（加窗），栅栏效应来自频率轴只在离散栅格上取样，二者不要混淆。加长有效数据记录可提高物理分辨率；在原记录后补零只能使频率采样更密、缓解栅栏效应，不能增加信息或物理分辨率。

---

## 五、数字滤波器基础

### 1. 分类与指标

按频率选择性可分为低通、高通、带通、带阻（陷波）滤波器。常用指标有通带/阻带边缘频率、通带纹波 $R_p$、阻带最小衰减 $A_s$ 和过渡带宽。

理想砖墙滤波器的冲激响应无限长且非因果，不能实时实现。实际设计是在频率选择性、相位、阶数和计算量之间折中。

### 2. FIR 与 IIR 的定义与比较

- **FIR（Finite Impulse Response，有限长冲激响应）滤波器**：单位冲激响应 $h[n]$ 仅在有限个样点上非零。典型 $N$ 阶 FIR 的输入输出关系为

$$
y[n]=\sum_{k=0}^{N}b_kx[n-k]
$$

  它只由当前及过去的输入样本构成，通常是前馈、非递归结构；只要系数有限，便天然 BIBO 稳定。其系统函数分母为常数，只有有限个零点、没有有限极点。

- **IIR（Infinite Impulse Response，无限长冲激响应）滤波器**：单位冲激响应 $h[n]$ 在理论上无限延伸。典型 $N$ 阶 IIR 的输入输出关系为

$$
y[n]=\sum_{k=0}^{M}b_kx[n-k]-\sum_{k=1}^{N}a_ky[n-k]
$$

  它含有过去的输出样本，通常是带反馈的递归结构；系统函数含有极点，因而必须保证极点位于单位圆内才能 BIBO 稳定。

| 对比项 | FIR | IIR |
| --- | --- | --- |
| 冲激响应 | 有限长 | 无限长 |
| 结构 | 一般无反馈 | 含反馈 |
| 稳定性 | 有限系数时天然 BIBO 稳定 | 必须检查极点 |
| 相位 | 可严格线性相位 | 通常非线性相位 |
| 阶数 | 同等选择性下往往较高 | 往往较低 |
| 量化敏感性 | 较低 | 极点位置对量化较敏感 |

在单位圆上令 $z=e^{j\omega}$ 可得到频率响应；幅度的分贝表示为 $A(\omega)=20\log_{10}|H(e^{j\omega})|$。注意模拟角频率 $\Omega$ 的单位为 rad/s，数字角频率 $\omega$ 的单位为 rad/sample，二者满足 $\omega=\Omega T_s$。

---

## 六、FIR 滤波器设计

### 1. 基本形式与线性相位

长度为 $N$ 的 FIR 滤波器为：

$$
y[n]=\sum_{k=0}^{N-1}h[k]x[n-k]
$$

若 $h[n]=h[N-1-n]$（对称）或 $h[n]=-h[N-1-n]$（反对称），则可实现广义线性相位。对称 FIR 常用于低通、高通、带通和带阻；反对称 FIR 常用于微分器、希尔伯特变换器等。

### 2. 窗函数法

窗函数法先由理想频响求无限长冲激响应 $h_d[n]$，再截断加窗：

$$
h[n]=h_d[n]w[n]
$$

矩形窗主瓣窄但旁瓣高；汉宁、海明、布莱克曼窗旁瓣更低但过渡带更宽。增加长度 $N$ 可使过渡带变窄；窗类型主要决定旁瓣高度和主瓣宽度的折中。

加窗会把理想频响的突变边沿变为过渡带，并使原本平坦的通带、阻带出现波纹（肩峰及余振）。仅增大截取长度不能改变给定窗函数主瓣与旁瓣的相对比例，故不能消除由窗型决定的相对波纹水平；要改变该折中需更换窗函数。

### 3. 频率采样法与等波纹法

- **频率采样法**：在等间隔频率点指定 $H[k]$，通过 IDFT 求 FIR 系数，适合频响离散给定的情形。
- **Parks–McClellan（等波纹）法**：采用最小最大误差准则，在给定通带/阻带纹波下通常可获得较低阶数，是常用的最优 FIR 设计方法。

### 4. 快速卷积

长 FIR 可用 FFT 分块计算。

- **重叠相加（OLA）**：输入分块补零，分别进行循环卷积，再将相邻块重叠的输出相加。
- **重叠保留（OLS）**：输入块保留前一块末尾的 $L_h-1$ 个样点，循环卷积后丢弃前 $L_h-1$ 个受时域混叠影响的输出。

二者都使 FFT 的循环卷积结果等价于线性卷积。

---

## 七、IIR 滤波器设计

### 1. 常用模拟低通原型

| 类型 | 通带 | 阻带 | 特点 |
| --- | --- | --- | --- |
| 巴特沃斯 | 最大平坦 | 单调 | 相同指标下过渡带较宽 |
| 切比雪夫 I 型 | 等波纹 | 单调 | 通带允许纹波，过渡更陡 |
| 切比雪夫 II 型 | 单调 | 等波纹 | 阻带允许纹波 |
| 椭圆（Cauer） | 等波纹 | 等波纹 | 同阶过渡最陡，相位较差 |

设计通常先由幅度指标确定模拟低通原型阶数，再完成低通到高通、带通或带阻的频率变换，并进行模拟到数字的变换。

### 2. 双线性变换法

双线性变换为：

$$
s=\frac{2}{T_s}\frac{1-z^{-1}}{1+z^{-1}}
$$

它将 $s$ 平面左半平面一一映射到 $z$ 平面单位圆内，故能保持稳定性，且虚轴映射到单位圆。缺点是频率非线性扭曲，关键数字频率须预畸变：

$$
\Omega=\frac{2}{T_s}\tan\frac{\omega}{2}
$$

### 3. 冲激响应不变法

冲激响应不变法通过采样模拟滤波器的冲激响应构造数字滤波器。它较好保留时域冲激响应形状，但模拟频谱会周期性重复，可能造成高频混叠，因此更适合带限或低通特性明显的原型。

### 4. IIR 稳定实现

IIR 极点必须位于单位圆内。高阶 IIR 通常分解为一阶或二阶节（SOS/biquad）级联，以降低系数量化和数值误差对稳定性的影响。高 Q 极点靠近单位圆时，对定点量化尤其敏感。

---

## 八、结构、有限字长与多速率处理

### 1. 常见实现结构

- **直接型 I**：分别保存输入和输出延迟，结构直观。
- **直接型 II**：合并两组延迟线，延迟单元最少，但内部节点动态范围可能较大。
- **级联型**：按一阶或二阶节分解，适于高阶 IIR，数值鲁棒性较好。
- **并联型**：部分分式展开后各支路输出相加，便于观察各极点贡献。
- **转置结构**：输入输出关系不变，但内部节点的量化噪声和溢出特性不同。

### 2. 有限字长效应

- **系数量化**：FIR 主要造成频响偏差；IIR 会使极点/零点移动，严重时可能失稳。
- **运算舍入**：截断或舍入引入量化噪声；舍入通常无偏，截断会引入偏差。
- **溢出**：定点系统常采用饱和或回绕；回绕失真严重，饱和通常更符合信号处理直觉。
- **极限环振荡**：IIR 在零输入下仍可能因量化非线性出现持续振荡。

可通过增加字长、合理缩放、使用 SOS 结构和高精度累加器减轻影响。

### 3. 多速率处理

- **抽取（降采样）**：先低通抗混叠，再每隔 $M$ 点取一个样点，采样率变为原来的 $1/M$。
- **插值（升采样）**：先插入 $L-1$ 个零，再低通抗成像，采样率变为原来的 $L$ 倍。
- **有理倍变换**：先插值 $L$ 倍再抽取 $M$ 倍，输出采样率为 $(L/M)f_s$。

降采样前的滤波器防止混叠；升采样后的滤波器去除插零造成的频谱镜像。

---

## 九、高频问答与易错点

### 1. DTFT、DFT 和 FFT 的区别？

DTFT 的时域离散、频域连续且周期；DFT 是有限长序列在 $N$ 个频点上的离散表示，隐含周期延拓；FFT 是快速计算 DFT 的算法，不是新的变换。

从对应关系看：一个域的周期性对应另一域的离散性；一个域的非周期性对应另一域的连续性。DTFT 也可视为 Z 变换在单位圆上的取值，DFT 则是 DTFT 在单位圆上等间隔的 $N$ 点采样。计算机需要离散的输入和输出，因此频谱数值计算通常采用 DFT（及其快速算法 FFT）。

### 2. 零填充能提高频率分辨率吗？

不能。零填充只加密频率采样点，使谱线显示更平滑、峰值更便于插值估计；真实分辨率由有效观测时长决定。

### 3. 线性卷积和循环卷积为何不同？

DFT 默认序列周期延拓，频域相乘对应循环卷积；普通 LTI 系统输出要求线性卷积。补零至 $N\ge L_x+L_h-1$ 后，两者可统一。

### 4. FIR 一定比 IIR 稳定吗？

有限系数 FIR 的冲激响应有限长，天然 BIBO 稳定；IIR 含反馈，必须保证极点位于单位圆内。实际实现中仍需考虑溢出和有限字长误差。

### 5. 为什么 IIR 往往比 FIR 阶数低？

IIR 用反馈引入极点，能以较少参数形成陡峭频率选择性；FIR 无反馈，若要求窄过渡带和高阻带衰减，通常需要更高阶数。

### 6. 双线性变换为什么无混叠？

它是 $s$ 平面到 $z$ 平面的非线性一一映射，虚轴到单位圆没有频率的周期性复制，因此不发生混叠；代价是频率扭曲，需要预畸变。

### 7. 如何从极点判断离散系统稳定性？

必须结合 ROC。对因果有理系统，所有极点位于单位圆内等价于稳定；一般情形的充要条件是 ROC 包含单位圆。

### 8. 加窗为何能减小泄漏却不能消除它？

加窗降低窗频谱旁瓣，减少能量向远处频点扩散；但主瓣仍有有限宽度，有限长度观测也仍会造成频域展宽，因此只是折中。

### 9. 采样率、采样点数与频率分辨率如何区分？

采样率 $f_s$ 决定最高无混叠频率约为 $f_s/2$；采样点数 $N$ 与采样率共同决定观察时长 $N/f_s$；DFT 频率间隔为 $\Delta f=f_s/N$。只提高 $f_s$ 而不增加 $N$，会扩大频率范围，却可能使频率间隔变粗。

### 10. 单边 Z 变换有什么用途？

它只考察 $n\geq0$ 的样本，因此两序列在非负时间部分相同就有相同的单边 Z 变换。其最大优点是能直接带入初始条件，适于求差分方程的零输入响应、零状态响应及完整瞬态响应。

### 11. 零极点靠近单位圆会怎样？

零点越靠近单位圆，对应频率处幅频响应的凹谷越深；极点越靠近单位圆，对应频率处的峰值越高、响应衰减越慢。改变零极点的分布即可塑造系统频率选择性。

### 12. 冲激响应不变法与双线性变换法如何比较？

冲激响应不变法令数字滤波器的单位脉冲响应等于模拟冲激响应的采样值，时域逼近好、频率坐标关系线性，但会因模拟频谱的周期延拓产生混叠，通常不适合高通和带阻设计。双线性变换是一一映射，不产生混叠，却带来频率扭曲；设计低通、高通、带通、带阻等选择性滤波器时，应在确定模拟原型前对关键边缘频率预畸变。

### 13. FIR 与 IIR 的性能取舍？

IIR 通常以较低阶数获得较高选择性，存储和运算量较小，但相位一般非线性，选择性越强时相位失真往往越明显。FIR 可严格线性相位且天然稳定，但为获得同等选择性通常阶数更高、延时更大；若相位线性要求严格，FIR 往往更合适。

### 14. 二维 FFT 如何计算，为什么常要中心化频谱？

二维 FFT 可先逐行做一维 FFT，再逐列做一维 FFT，利用二维 DFT 的可分离性降低计算量。原始二维 DFT 的零频位于左上角；将其移到中心只是为了让低频居中、便于观察和设计频域滤波器，并不改变频谱所含的信息。二维 FFT 本质仍是计算二维 DFT 的快速算法。

---

## 附：重要公式汇总表

| 内容 | 公式/结论 |
| --- | --- |
| 离散卷积 | $y[n]=\sum_kx[k]h[n-k]$ |
| DTFT | $X(e^{j\omega})=\sum_nx[n]e^{-j\omega n}$ |
| IDTFT | $x[n]=\frac1{2\pi}\int_{-\pi}^{\pi}X(e^{j\omega})e^{j\omega n}d\omega$ |
| Z 变换 | $X(z)=\sum_nx[n]z^{-n}$ |
| DFT | $X[k]=\sum_{n=0}^{N-1}x[n]e^{-j2\pi kn/N}$ |
| IDFT | $x[n]=\frac1N\sum_{k=0}^{N-1}X[k]e^{j2\pi kn/N}$ |
| 频率分辨率 | $\Delta f=f_s/N$ |
| 奈奎斯特采样条件 | $f_s\geq2f_m$（工程中通常留裕量） |
| 理想量化信噪比 | $\mathrm{SQNR}\approx6.02B+1.76\,\mathrm{dB}$ |
| 群延迟 | $\tau_g(\omega)=-d\angle H(e^{j\omega})/d\omega$ |
| 双线性变换 | $s=\frac{2}{T_s}\frac{1-z^{-1}}{1+z^{-1}}$ |
| 预畸变 | $\Omega=\frac{2}{T_s}\tan(\omega/2)$ |
