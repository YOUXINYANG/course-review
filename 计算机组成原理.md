# 计算机组成原理

> 面向复试的高频问答。回答时优先讲清“是什么—为什么—怎么做”，再按老师追问补充细节。

## 目录

- [一、计算机系统概述](#一计算机系统概述)
- [二、存储系统](#二存储系统)
- [三、CPU 与指令系统](#三cpu-与指令系统)
- [四、总线与输入输出](#四总线与输入输出)
- [常见面试提问](#常见面试提问)
---

## 一、计算机系统概述

### 1. 计算机的基本结构

- **五大部件**：运算器、控制器、存储器、输入设备和输出设备；CPU 由运算器和控制器构成。
- **存储程序**：程序和数据以二进制形式存于主存，CPU 按 `PC` 取指、译码、执行。
- **数据表示**：有符号整数通常用补码；浮点运算依次经历对阶、尾数运算、规格化、舍入和判溢。

## 二、存储系统

寄存器、Cache、主存和辅存构成由快到慢、由小到大的层次。Cache 缓解 CPU 与主存的速度差，虚拟存储器缓解主存与辅存的容量差，二者都利用时间和空间局部性。Cache 映射可分直接映射、全相联和组相联；写策略包括写直通和写回。

## 三、CPU 与指令系统

指令由操作码和地址字段组成。CPU 经取指、译码、执行、访存和写回完成指令；`PC` 指向下一条指令，`IR` 保存当前指令，`MAR/MDR` 负责主存地址和数据交换。流水线受结构、数据和控制冒险限制。

## 四、总线与输入输出

地址线传送地址，数据线传送数据；同步总线依靠统一时钟，异步总线依靠握手。DMA 由控制器直接在设备与主存之间成块传输，减少 CPU 参与。

---

## 常见面试提问

#### 一、计算机系统与数据表示

#### 1. 什么是冯·诺依曼计算机？

**核心是存储程序。**程序和数据都以二进制形式存入主存；CPU 按 PC 指出的地址取指、解释并执行。经典结构由运算器、控制器、存储器、输入设备和输出设备组成，传统上以运算器为中心，现代计算机更强调以存储器为中心的数据通路。

#### 2. 编译和解释有什么区别？

- **编译**：先把整个源程序翻译为目标程序，再运行；运行时开销小，便于优化。
- **解释**：运行时逐句或逐段翻译并执行；便于交互和调试，但通常执行开销更大。

实际语言实现常混合两者，例如先编译为字节码，再由虚拟机解释或即时编译。

#### 3. 原码、反码、补码和移码分别有什么用？

- **原码**：符号位与数值位分开，直观，但零有两种表示，运算不方便。
- **反码**：负数为原码数值位取反；历史上的过渡表示。
- **补码**：负数为反码加一；零唯一，减法可统一为加法，是整数运算的主流表示。
- **移码**：补码符号位取反，大小比较方便，常用于浮点数的阶码。

#### 4. 补码如何完成减法？如何判断溢出？

`x - y = x + (-y)`，硬件只需实现加法器和求补即可。补码加法中，**同号相加得异号结果**才溢出；等价地，最高有效位的进位与符号位进位不同则溢出。无符号数则用最高位进位判断是否溢出。

#### 5. 逻辑移位和算术移位有什么区别？

- **逻辑移位**将操作数视为无符号数，空位一律补 `0`。
- **算术右移**用于有符号补码数，左侧补符号位以近似保持数值除以 2；算术左移通常补 `0`，但可能溢出。

#### 6. 浮点数一次加减包含哪些步骤？

通常依次为：**对阶、尾数加减、规格化、舍入、溢出判断**。对阶时小阶向大阶对齐，尾数右移；规格化保证尾数落在约定范围内。IEEE 754 中阶码采用移码，尾数采用规格化有效数表示。

#### 7. 海明码如何确定校验位数？

若有 `n` 位有效信息、`k` 位校验位，要能定位“无错或任意一位出错”，需满足 `2^k >= n + k + 1`。标准海明码可纠正一位错；再加一位整体奇偶校验可做到**纠正一位、检测两位**。

#### 8. 大端和小端存储是什么？

多字节数据在低地址处存放高字节的是**大端**，存放低字节的是**小端**。它只影响字节排列，不改变寄存器中的数值；跨机器传输或按字节访问时必须统一约定。

#### 9. C 结构体为什么要对齐？

常见规则是成员起始地址按其对齐要求对齐，结构体总大小再向最大对齐要求取整。这样可减少一次访问跨越多个存储单元的情况，提高硬件访存效率；代价是出现填充字节。

---

### 二、存储系统

#### 1. ROM、SRAM 和 DRAM 的区别？

- **ROM**通常非易失，用于固件等只读或少写内容。
- **SRAM**用触发器存储，不需刷新、速度快、密度低且贵，常做 Cache。
- **DRAM**用电容存储，需要周期性刷新、密度高且便宜，常做主存。

#### 2. 存储元、存储单元和地址分别是什么？

存储元保存一位二进制信息；若干存储元组成可独立编址的**存储单元**；存储单元的唯一编号是**地址**。存储字长是一个存储单元包含的位数，机器字长是 CPU 一次自然处理的位数，指令字长是单条指令的位数。

#### 3. 存储器如何扩展？

- **位扩展**：并联多个芯片，增加每个单元的位数。
- **字扩展**：用地址译码选择芯片，增加单元个数。
- **字位扩展**：两者结合。

#### 4. 什么是 Cache？它为什么有效？

Cache 是 CPU 与主存之间的小容量高速存储器，保存近期可能再次访问的主存块。它利用**时间局部性**和**空间局部性**：命中时直接由 Cache 提供数据，未命中才访问主存，从而降低平均访存时间。

#### 5. Cache 有哪些映射方式？

- **直接映射**：一个主存块只能映射到一个 Cache 行，简单快，但冲突缺失多。
- **全相联映射**：可放任意行，冲突少，但比较器多、代价高。
- **组相联映射**：先按索引定位组，再在组内全相联查找，是常用折中。

#### 6. 写直通和写回如何选择？

**写直通**每次写 Cache 同时写主存，主存始终较新、实现简单，但总线流量大，常配写缓冲。**写回**只写 Cache，脏块替换时再写主存，带宽效率高但需脏位，控制更复杂。写未命中还可选写分配或非写分配。

#### 7. Cache 与虚拟存储器的异同？

二者都基于局部性、都以较小高速层模拟较大低速层。不同在于：Cache 位于 CPU 与主存之间，通常由硬件自动管理，目标是缩短平均访存时间；虚拟存储器位于主存与辅存之间，由软硬件协作，目标是扩展可用地址空间。

#### 8. MMU 和 TLB 的作用？

MMU 负责把虚拟地址转换为物理地址并实施保护。TLB 是页表项的高速缓存：TLB 命中可快速完成地址转换；未命中再查页表，若页不在内存则产生缺页异常。

---

### 三、指令系统与 CPU

#### 1. 一条指令由什么组成？

通常包括**操作码**和**地址码/地址字段**；地址字段还隐含或编码寻址方式。有效地址 EA 是最终操作数所在的地址，立即数和寄存器寻址可不访问主存。

#### 2. 常见寻址方式及用途？

- 直接、间接、寄存器、寄存器间接：在地址与访问次数间取舍。
- **相对寻址**：`EA = PC + 位移`，适合转移指令和位置无关代码。
- **基址寻址**：`EA = 基址寄存器 + 位移`，适合重定位与多道程序。
- **变址寻址**：`EA = 变址寄存器 + 位移`，适合数组和循环。

#### 3. CPU 如何区分指令和数据？

内存中的比特本身没有类型。CPU 由当前阶段和控制信号决定其含义：取指周期把 PC 指向内容送入 IR 并按指令解释；执行周期按指令需要把内容当作操作数读写。

#### 4. 指令周期、机器周期、总线周期和时钟周期？

**时钟周期**是最基本节拍；**机器周期**完成一次基本操作（如访存）；**总线周期**完成一次总线传输；**指令周期**从取一条指令到取下一条指令之间的全部时间，通常包含若干机器周期。不同机器的具体对应关系并不固定。

#### 5. 机器指令和微指令的关系？

机器指令是 ISA 向程序员暴露的指令；微指令是控制器内部发出的微操作控制信号。一条机器指令可由一个微程序的多条微指令实现。硬布线控制速度高但修改困难；微程序控制更易扩展，常与复杂指令集结合。

#### 6. 寄存器 PC、IR、MAR、MDR 分别做什么？

- `PC`：下一条指令地址。
- `IR`：当前正在译码/执行的指令。
- `MAR`：本次主存访问地址。
- `MDR`：本次从主存读出或将写入主存的数据。

#### 7. 指令流水线为什么会阻塞？

三类冒险：**结构冒险**（资源争用）、**数据冒险**（结果尚未产生便被使用）、**控制冒险**（分支去向未知）。缓解方式包括资源复制、转发、暂停/插入气泡、分支预测和投机执行。

#### 8. ALU 与控制器分别负责什么？

ALU 执行算术、逻辑、移位和比较等数据操作；控制器根据指令和状态产生控制信号，组织取指、译码、执行、访存和写回。二者与寄存器、内部总线共同构成 CPU 数据通路。

---

### 四、总线与 I/O

#### 1. 地址线和数据线的作用？

地址线指出被访问的存储单元或 I/O 端口；数据线传送指令、数据和状态信息。地址线宽度决定可寻址范围，数据线宽度影响一次传输的数据量。

#### 2. 同步总线和异步总线？

同步总线依靠统一时钟，控制简单、时序确定，但速度受最慢设备约束；异步总线用请求/应答握手，适合不同速度设备，但控制开销较大。

#### 3. 什么是总线仲裁和总线带宽？

仲裁决定多个主设备同时申请时谁获得总线，可分集中式和分布式。总线带宽是单位时间可传输的数据量，粗略可由“数据线宽度 × 有效传输频率”估计，实际还受协议和等待周期影响。

#### 4. 什么是突发传输？

一次给出起始地址后，连续传输多个相邻数据单元，减少重复寻址和仲裁开销。Cache 填充一个块、DMA 成块搬运都常使用突发传输。

#### 5. DMA 是什么？为什么优先级通常高于 CPU？

DMA 控制器在 CPU 初始化后直接在设备与主存间传送一块数据，仅在开始或结束时中断 CPU。I/O 数据若不能及时搬运可能丢失，因此 DMA 往往有较高总线优先级；它可采用周期挪用、交替访问或突发方式与 CPU 共享主存。

#### 6. 磁盘访问时间由哪些部分构成？

主要为**寻道时间**（磁头移到目标磁道）、**旋转延迟**（目标扇区转到磁头下）和**传输时间**。题目给定具体参数时三者相加即可。

---

### 五、复试速答

#### 1. 内存和 Cache 的区别？

主存通常由 DRAM 构成，容量大、速度较慢；Cache 多由 SRAM 构成，容量小、速度快，自动缓存主存热点数据。两者共同构成层次化存储系统。

#### 2. USB 有哪些优点？

支持即插即用和热插拔，具有统一接口、供电能力、较强扩展性及较高传输速率。它属于外设互连总线，具体速率和拓扑取决于版本。

#### 3. 什么是虚拟机？

虚拟机在一台物理机上模拟多个相互隔离的计算环境，每个环境可独立运行操作系统和应用。它常用于测试、隔离、资源整合和部署；代价是有虚拟化开销。
