# 计算机组成原理

> 本文按 408 的知识体系梳理计算机组成原理；文末保留面向保研复试的高频问答，便于最后阶段速答复习。

## 目录

- [一、计算机系统概述](#一计算机系统概述)
- [二、数据的表示和运算](#二数据的表示和运算)
- [三、存储系统](#三存储系统)
- [四、指令系统](#四指令系统)
- [五、中央处理器](#五中央处理器)
- [六、总线系统](#六总线系统)
- [七、输入输出系统](#七输入输出系统)
- [八、性能、可靠性与易混概念](#八性能可靠性与易混概念)
- [常见面试提问](#常见面试提问)

---

## 一、计算机系统概述

### 1. 计算机系统的层次结构

计算机系统由硬件和软件组成。硬件是物理设备；软件分为系统软件和应用软件。操作系统管理资源，编译程序把高级语言翻译为机器可执行形式，汇编程序把汇编语言翻译为机器语言。

从下到上可把计算机看成微程序机器、传统机器语言机器、操作系统机器、汇编语言机器和高级语言机器。上层以更友好的方式使用下层功能。**指令系统（ISA）**是软硬件之间的重要接口：它规定机器指令、数据类型、寄存器、寻址方式和异常等，对编译器和程序员可见。

### 2. 冯·诺依曼计算机

冯·诺依曼结构包括运算器、控制器、存储器、输入设备和输出设备；运算器与控制器合称 CPU。其核心是**存储程序**：程序和数据都用二进制表示并存放在存储器中，CPU 按地址顺序取出指令、译码并执行。现代计算机通常以存储器为中心组织数据流，但仍遵循这一基本思想。

程序与数据在物理上通常不作区别，区别来自 CPU 当前的访问方式：取指阶段把读出的位串解释为指令，执行阶段则可能把位串解释为操作数。指令和数据共用存储器、总线会形成冯·诺依曼瓶颈，Cache、流水线和更宽的总线都是缓解手段。

### 3. 基本工作过程与性能指标

CPU 中的 `PC` 保存下一条指令地址。取指时，地址送入 `MAR`，主存读出的指令经 `MDR` 送入 `IR`；控制器对 `IR` 译码，发出控制信号，驱动寄存器、ALU、主存和 I/O 接口完成操作。`PC` 随后更新到顺序下一条或转移目标地址。

- `PC`：下一条指令地址；`IR`：当前指令；
- `MAR`：本次主存访问地址；`MDR`：本次从主存读出或待写入主存的数据；
- 通用寄存器、`ACC`、`SP` 等暂存操作数、中间结果或专用地址。

主频 $f$ 的倒数是时钟周期 $T$。CPU 执行时间为：

$$
T_{CPU}=\text{指令条数}\times CPI\times T
$$

其中 `CPI` 是平均每条指令的时钟周期数。提高主频不必然提高性能；指令条数、CPI、访存延迟和并行度同样重要。

---

## 二、数据的表示和运算

### 1. 进位计数制与定点数

二进制、八进制、十六进制都是按位权展开的进位计数制。二进制与八进制、十六进制之间可按 3 位、4 位直接转换。`n` 位无符号整数的范围是 $0\sim2^n-1$。

定点数的小数点位置固定。原码用最高位表示符号，直观但有两种零且加减不便；反码是负数数值位取反；补码是反码加一。`n` 位补码整数范围为 $-2^{n-1}\sim2^{n-1}-1$，零唯一，并把减法统一为加法。

补码中，负数取反加一即可得到相反数；但最小负数 $-2^{n-1}$ 的相反数超出 `n` 位补码范围。符号扩展时，正数高位补 `0`，负数高位补 `1`，数值不变。

### 2. 定点数运算

补码减法按 $x-y=x+(-y)$ 完成。补码加法中，**两个同号数相加而结果异号时溢出**；等价地，符号位的进位与最高数值位的进位不同。无符号数加法则以最高位是否产生进位判断溢出。

逻辑移位把操作数视为无符号数，空位均补 `0`。算术右移用于补码有符号数，左侧补符号位；算术左移通常低位补 `0`，但可能溢出。移位可高效实现乘除 $2^k$，但有符号除法的舍入还要结合具体 ISA 判断。

乘法本质上是根据乘数各位进行移位累加；无符号数可用原码一位乘法，补码数常用 Booth 算法减少连续 `1` 时的加法次数。除法本质上是逐位试商和移位减法；恢复余数法在试减为负时恢复余数，不恢复余数法减少恢复操作。

### 3. 浮点数与 IEEE 754

浮点数一般写作 $N=(-1)^S\times M\times R^E$，由符号、阶码和尾数组成。阶码决定数量级，通常采用移码；尾数决定有效数字。规格化使尾数落在约定范围内，从而最大化有效精度。

IEEE 754 单精度占 32 位：1 位符号位、8 位阶码、23 位尾数；双精度占 64 位：1、11、52 位。对规格化数，实际阶码为“存储阶码减偏置”，有效尾数隐含最高位 `1`。阶码全 `0` 用于零和非规格化数，阶码全 `1` 用于无穷和 NaN。

浮点加减依次经历**对阶、尾数运算、规格化、舍入、溢出判断**。对阶时小阶向大阶对齐，尾数右移会损失精度。浮点数不能精确表示多数十进制小数，比较时常应使用允许误差。

### 4. 校验码

奇偶校验码只能检出奇数位错误，不能定位或纠正错误。海明码在数据位中插入校验位；若有 `n` 位信息位、`k` 位校验位，纠正一位错需满足 $2^k\ge n+k+1$。校验位位置通常为 $2^i$，接收端的校验结果构成错误位号。加一位整体奇偶校验后，可实现**纠正一位错、检测两位错**。

---

## 三、存储系统

### 1. 存储器的层次与指标

寄存器、Cache、主存和辅存构成由快到慢、由小到大的层次。上层缓存下层近期常用内容，利用时间局部性和空间局部性，以较小成本获得接近高速存储器的平均性能。Cache 主要解决 CPU 与主存的速度差；虚拟存储器主要解决主存与辅存的容量差。

存储容量常写为“字数 × 字长”。地址线宽度为 `m` 位时最多可选择 $2^m$ 个地址；数据线宽度决定一次传输的位数。存取时间是启动一次读写到数据稳定的时间，存取周期是两次独立访问的最小间隔；带宽约为每次传输字节数除以存取周期。

### 2. 半导体主存与扩展

ROM 通常非易失，适合固件；RAM 断电后数据丢失。SRAM 由触发器存储，不需刷新、速度快、集成度低且价格高，多用于 Cache；DRAM 用电容存储，需要刷新、容量大且成本低，多用于主存。DRAM 刷新可采用集中、分散或异步刷新，刷新期间相关存储体不能正常读写。

存储芯片扩展包括位扩展、字扩展和字位扩展。位扩展将多个芯片的数据端并联以增加字长；字扩展用高位地址经译码器选择芯片以增加字数；字位扩展同时使用两者。连接时要明确地址线、数据线、片选线和读写控制线的去向。

### 3. Cache

Cache 以块（Cache line）为单位从主存调入数据。主存地址通常划分为标记、组号（或行号）和块内地址。命中时直接读写 Cache；未命中时从主存装入相应块，必要时替换旧块。

- **直接映射**：一个主存块只能放到唯一 Cache 行，实现简单但冲突缺失较多；
- **全相联映射**：主存块可放任一行，冲突少但需并行比较所有标记；
- **组相联映射**：先由组号定位一组，再在组内比较标记，是成本和命中率的折中。

直接映射无需替换算法。写直通在写 Cache 时同步写主存，实现简单但总线流量大，常配写缓冲；写回仅更新 Cache，脏块被替换时才写主存，带宽利用更好但控制较复杂。写未命中时，写分配先将块调入 Cache，非写分配则直接写主存。

#### Cache 替换算法

替换算法决定组相联或全相联 Cache 在目标组已满时淘汰哪一条 Cache 行。直接映射中每个主存块只有唯一位置，因此不需要替换算法。

- **最优替换（OPT）**：淘汰未来最长时间内不会再次访问，或未来最晚才访问的块。它能达到理论最高命中率，但需要预知未来访问序列，只用于理论分析和模拟。
- **先进先出（FIFO）**：按照 Cache 行进入的先后顺序替换，最早进入的块最先淘汰。实现简单、开销小，但可能淘汰一直被频繁访问的块，并可能出现容量增大而命中率下降的 Belady 异常。
- **最近最少使用（LRU）**：淘汰最长时间未被访问的块，利用时间局部性，通常比 FIFO 命中率高。严格记录访问顺序的硬件开销随相联度增加，因此更适合低路数 Cache。
- **最近最不常使用（LFU）**：淘汰累计访问次数最少的块，适合访问频率差异明显的场景；但历史高频块可能长期占据 Cache，通常需要计数器衰减或老化机制。
- **随机替换（Random）**：随机选择一条 Cache 行淘汰，硬件开销低、访问延迟稳定，但结果不可预测，可能淘汰刚被访问的热点块。
- **未近期使用（NRU）**：为每条 Cache 行设置引用位，访问时置 `1`，周期性清零，优先淘汰引用位为 `0` 的块。它只粗略近似 LRU，实现简单但区分能力有限。
- **伪 LRU（PLRU）**：使用少量树形或位状态近似记录访问顺序，在较高相联度下比严格 LRU 节省硬件，但不保证淘汰真正最久未使用的块。

常见算法的特点如下：

| 算法 | 替换依据 | 实现开销 | 典型特点 |
| --- | --- | --- | --- |
| OPT | 未来访问时间 | 无法实现 | 理论最优基准 |
| FIFO | 进入时间 | 低 | 简单，但可能有 Belady 异常 |
| LRU | 最近访问时间 | 较高 | 命中率较好，低路数常用 |
| LFU | 累计访问次数 | 较高 | 需处理历史访问记录 |
| Random | 随机选择 | 很低 | 简单稳定但不可预测 |
| NRU/PLRU | 近期访问的近似信息 | 低到中等 | 以较小开销近似 LRU |

替换策略、写策略和写未命中策略需要区分：替换策略决定淘汰哪一行；写直通或写回决定如何更新主存；写分配或非写分配决定写未命中时是否先把目标块调入 Cache。
平均访存时间：

$$
AMAT=\text{命中时间}+\text{缺失率}\times\text{缺失代价}
$$

缺失可分强制缺失、容量缺失和冲突缺失。增大 Cache 不一定总有效：容量、相联度和块大小会同时影响命中率、命中时间与缺失代价。

### 4. 虚拟存储器

虚拟存储器通过地址转换让程序看到连续且比物理内存更大的虚拟地址空间。分页系统中，虚拟地址由页号和页内偏移组成；页表把虚页号映射为物理页框号。页不在主存时触发缺页异常，由操作系统从辅存调入页面，必要时置换一个页框。

多级页表只为实际使用的地址空间建立下级页表，节省页表空间；反向页表按物理页框组织，适合很大的物理内存。

### 5. MMU

MMU（内存管理单元）负责把虚拟地址转换为物理地址，并在访存时检查页表项中的有效位、读写权限和用户/内核权限等保护信息。地址转换或权限检查失败时，MMU 会触发相应异常，由操作系统处理。

### 6. TLB

TLB（快表）是页表项的高速缓存，用于避免每次访存都访问主存中的页表。TLB 命中时可快速得到物理页框号；TLB 未命中时才访问页表。若页表项表明该页不在主存，则产生缺页异常；TLB 未命中本身不等于缺页。

---

## 四、指令系统

### 1. 指令格式与指令类型

机器指令由操作码和地址码字段构成。操作码决定操作类型，地址码给出操作数地址、寄存器编号、立即数或寻址方式。操作码可以定长，也可以采用扩展操作码：常用指令采用较短编码，较少用指令占用较长编码。

指令按功能可分数据传送、算术逻辑、移位、转移、输入输出和特权指令。指令字长、机器字长、存储字长并不必然相同；变长指令格式能提高编码密度，但取指与译码更复杂。

### 2. 寻址方式

寻址方式决定如何获得操作数或有效地址（EA）。立即寻址的操作数就在指令中；寄存器寻址直接访问寄存器；直接寻址的地址字段就是 EA；间接寻址需根据地址字段再访问一次存储器；寄存器间接寻址以寄存器内容为 EA。

相对寻址 $EA=PC+\text{位移}$，适合分支和位置无关代码；基址寻址 $EA=BR+\text{位移}$，适合程序重定位；变址寻址 $EA=IX+\text{位移}$，适合数组；基址变址寻址把两类寄存器结合。判断 `PC` 取何值时，要注意题目约定的是当前指令地址还是下一条指令地址。

### 3. CISC 与 RISC

CISC 指令多、格式和寻址方式复杂，一条指令可能完成多个步骤，常采用微程序控制；RISC 采用少量规则、定长、易译码的指令，强调 Load/Store 结构和大量通用寄存器，利于流水线。现代处理器常在 ISA 层兼容复杂指令，而在内部转换成较简单的微操作执行，二者并非绝对对立。

---

## 五、中央处理器

### 1. CPU 的组成与数据通路

CPU 由 ALU、寄存器组、控制器和内部互连组成。ALU 完成算术、逻辑、移位和比较；寄存器组保存操作数和结果；控制器按指令和状态产生控制信号。数据通路描述数据在寄存器、ALU、总线和主存间的流动，控制信号决定何时读、写、选通和运算。

单总线数据通路硬件少，但同一时刻总线只能传送一份数据，微操作需分时完成；多总线结构可并行传送更多数据，速度较高但成本和控制复杂度更大。条件码寄存器常保存零、负、进位、溢出等标志，供条件转移使用。

### 2. 指令周期与微操作

一条指令通常经过取指、间址（若需要）、执行和中断等阶段。取指的典型微操作为：`MAR ← PC`，主存读，`IR ← MDR`，`PC ← PC + 1`（具体增量由指令编址单位决定）。执行阶段因指令不同而不同；访存型指令还会经历有效地址计算和读写主存。

时钟周期是最小时间单位；机器周期完成一次基本操作，如一次主存访问；指令周期是执行一条指令的总时间，包含若干机器周期。总线周期是一次总线传输所占时间，和机器周期的对应关系取决于具体机器实现。

### 3. 控制器

硬布线控制器由组合逻辑、时序和状态逻辑直接产生控制信号，速度快但设计、修改困难，适合指令较规整的场景。微程序控制器把控制信号组织成微指令并存于控制存储器；一条机器指令对应一段微程序，易于设计和修改，但通常速度较慢。

微指令的控制字段可以直接编码，也可以字段编码。直接编码的控制位多、并行性好；字段编码压缩控制存储器但需译码，互斥的微命令可放在同一字段。微程序顺序控制可通过顺序地址、条件转移地址、机器指令操作码映射地址等方式确定下一条微指令。

### 4. 指令流水线

流水线把指令执行分成若干阶段，不同指令在不同阶段重叠执行。理想情况下，`k` 段流水线执行 `n` 条指令所需时间约为 $(k+n-1)\Delta t$，吞吐率接近每个时钟周期完成一条指令；单条指令的延迟并未按段数缩短。

流水线冒险包括结构冒险（争用同一硬件资源）、数据冒险（需要的结果尚未产生）和控制冒险（分支目标未知）。结构冒险可增加资源或错开使用；数据冒险可用转发、暂停或编译器调度缓解；控制冒险可用尽早判定分支、分支预测、延迟槽或投机执行缓解。经典五段顺序流水线主要出现 RAW（写后读）冒险，乱序执行还需处理 WAR、WAW。

### 5. 中断与异常

中断会使 CPU 暂停当前控制流，保存断点和必要现场，转入服务程序，完成后恢复并返回。外中断来自处理器外部，如 I/O 完成和时钟；异常（内中断）由当前指令同步引起，如除零、越界、缺页和非法指令；陷入通常指程序执行系统调用指令而主动进入内核。

中断响应通常包括关中断或判优、保存断点、取中断向量、保存现场、执行服务程序、恢复现场和中断返回。可屏蔽中断能由中断允许位暂时屏蔽，不可屏蔽中断用于紧急事件；中断嵌套时应明确优先级和现场保存范围。

---

## 六、总线系统

### 1. 总线的概念与分类

总线是一组共享传输线和传输协议，用于在部件间传送信息。按功能可分地址总线、数据总线和控制总线；按连接范围可分片内总线、系统总线和通信总线。地址线宽度影响最大可寻址空间，数据线宽度影响一次传输的数据量，但实际带宽还受时钟、传输周期和协议开销影响。

单总线结构简单、成本低，但所有部件竞争同一通路；多总线结构并行性好但硬件复杂。总线一次传输通常经历申请、仲裁、寻址、数据传送和结束等阶段。

### 2. 总线仲裁与通信方式

当多个主设备申请总线时，需要仲裁决定使用权。集中式仲裁由专门仲裁器决定，常见链式查询、计数器定时查询和独立请求；分布式仲裁由各主设备共同决定。仲裁需要在优先级、响应速度、公平性和硬件成本间权衡。

同步通信依靠统一时钟，控制简单、定时确定，但周期需迁就最慢设备；异步通信用请求/应答握手，不依赖统一时钟，适合速度差异大的设备。半同步通信在统一时钟下允许插入等待周期。突发传输只给出一次起始地址，连续传输多个相邻数据，适合 Cache 填充和 DMA。

---

## 七、输入输出系统

### 1. I/O 接口与编址

I/O 接口连接 CPU/总线与外设，通常包含数据寄存器、状态寄存器和控制寄存器，用于缓冲数据、反映设备状态、接收命令并完成时序匹配。外设速度和数据格式往往与 CPU 不同，接口还承担转换、缓冲和错误检测。

I/O 编址可采用独立编址或统一编址。独立编址为 I/O 设置独立地址空间和专用 I/O 指令；统一编址把 I/O 寄存器映射到主存地址空间，能复用访存指令和寻址方式，但占用内存地址。

### 2. I/O 方式

程序查询方式由 CPU 反复读取状态寄存器，简单但忙等浪费 CPU。程序中断方式由设备在就绪或完成时请求 CPU 服务，CPU 可在等待期间执行其他程序，但每次传送仍需 CPU 参与。DMA 方式由 DMA 控制器直接在设备与主存间成块传输，CPU 只负责初始化和结束处理，适合高速、大批量数据。

DMA 控制器通常有地址寄存器、字计数器和数据缓冲寄存器。CPU 设置传送方向、主存起始地址和数据量后，DMA 申请总线、传送一个或多个字、更新地址与计数，完成后以中断通知 CPU。DMA 可采用周期挪用、交替访问和突发传送等方式与 CPU 共享总线。通道是功能更强的专用 I/O 处理器，能执行通道程序并管理多次 I/O 操作。

### 3. 外存与 RAID

磁盘访问时间由寻道时间、旋转延迟和传输时间组成。磁盘的盘面、磁道、扇区、柱面是常见物理组织单位；扇区通常是最小读写单位。固态硬盘没有机械寻道和旋转延迟，但仍受闪存擦写、磨损均衡和控制器影响。

RAID 把多块磁盘组合以提高性能、容量或可靠性。RAID 0 通过条带化提高并行性能但无冗余；RAID 1 镜像冗余高、容量利用率低；RAID 5 使用分布式奇偶校验，可容忍一块盘故障；RAID 6 使用双重校验，可容忍两块盘故障。RAID 不能替代备份。

---

## 八、性能、可靠性与易混概念

### 1. 性能分析

性能通常以执行时间衡量，性能与执行时间成反比。比较两个系统时，应使用相同任务和相同测试条件。Amdahl 定律说明：若某部分占原执行时间比例为 `p`，该部分加速比为 `s`，总体加速比为：

$$
S=\frac{1}{(1-p)+p/s}
$$

因此应优先优化耗时占比大的部分；任何局部优化都受未优化部分限制。流水线、Cache、多核和更快 I/O 的收益都应放在完整系统中评估。

### 2. 可靠性

可靠性是系统在规定条件和时间内正确工作的概率。平均无故障时间（MTTF）越大，平均修复时间（MTTR）越小，可用性通常越高，近似为 $MTTF/(MTTF+MTTR)$。冗余、纠错码、校验、故障隔离和备份是提高可靠性的常用手段，但会增加成本、复杂度或性能开销。

### 3. 容易混淆的概念

- **Cache 与虚拟存储器**：前者位于 CPU 与主存之间，主要降低平均访问时间，通常由硬件自动管理；后者位于主存与辅存之间，主要扩展地址空间，需要操作系统参与。
- **中断与 DMA**：中断是通知 CPU 的控制机制；DMA 是数据传送方式。DMA 完成后通常仍通过中断通知 CPU。
- **异常与外中断**：异常由当前指令同步引起；外中断由处理器外部事件异步引起。
- **存储字长、机器字长、指令字长**：分别是一个存储单元位数、CPU 一次自然处理的数据位数和一条指令的位数，三者不必相等。
- **主存地址与有效地址**：主存地址是实际访问存储单元的地址；有效地址是指令按寻址方式计算出的操作数地址，在虚拟存储器中还可能继续转换为物理地址。

---
## 常见面试提问

#### 一、计算机系统与数据表示

#### 1. 什么是冯·诺依曼计算机？

**核心是存储程序。**程序和数据都以二进制形式存入主存；CPU 按 PC 指出的地址取指、解释并执行。经典结构由运算器、控制器、存储器、输入设备和输出设备组成，传统上以运算器为中心，现代计算机更强调以存储器为中心的数据通路。

#### 2. 编译和解释有什么区别？

- **编译**：先把整个源程序翻译为目标程序，再运行；运行时开销小，便于优化。
- **解释**：运行时逐句或逐段翻译并执行；便于交互和调试，但通常执行开销更大。

实际语言实现常混合两者，例如先编译为字节码，再由虚拟机解释或即时编译。

#### 3. 原码、反码、补码和移码分别有什么用？

- **原码**：符号位与数值位分开，直观，但零有两种表示，运算不方便。
- **反码**：负数为原码数值位取反；历史上的过渡表示。
- **补码**：负数为反码加一；零唯一，减法可统一为加法，是整数运算的主流表示。
- **移码**：补码符号位取反，大小比较方便，常用于浮点数的阶码。

#### 4. 补码如何完成减法？如何判断溢出？

`x - y = x + (-y)`，硬件只需实现加法器和求补即可。补码加法中，**同号相加得异号结果**才溢出；等价地，最高有效位的进位与符号位进位不同则溢出。无符号数则用最高位进位判断是否溢出。

#### 5. 逻辑移位和算术移位有什么区别？

- **逻辑移位**将操作数视为无符号数，空位一律补 `0`。
- **算术右移**用于有符号补码数，左侧补符号位以近似保持数值除以 2；算术左移通常补 `0`，但可能溢出。

#### 6. 浮点数一次加减包含哪些步骤？

通常依次为：**对阶、尾数加减、规格化、舍入、溢出判断**。对阶时小阶向大阶对齐，尾数右移；规格化保证尾数落在约定范围内。IEEE 754 中阶码采用移码，尾数采用规格化有效数表示。

#### 7. 海明码如何确定校验位数？

若有 `n` 位有效信息、`k` 位校验位，要能定位“无错或任意一位出错”，需满足 `2^k >= n + k + 1`。标准海明码可纠正一位错；再加一位整体奇偶校验可做到**纠正一位、检测两位**。

#### 8. 大端和小端存储是什么？

多字节数据在低地址处存放高字节的是**大端**，存放低字节的是**小端**。它只影响字节排列，不改变寄存器中的数值；跨机器传输或按字节访问时必须统一约定。

#### 9. C 结构体为什么要对齐？

常见规则是成员起始地址按其对齐要求对齐，结构体总大小再向最大对齐要求取整。这样可减少一次访问跨越多个存储单元的情况，提高硬件访存效率；代价是出现填充字节。

---

### 二、存储系统

#### 1. ROM、SRAM 和 DRAM 的区别？

- **ROM**通常非易失，用于固件等只读或少写内容。
- **SRAM**用触发器存储，不需刷新、速度快、密度低且贵，常做 Cache。
- **DRAM**用电容存储，需要周期性刷新、密度高且便宜，常做主存。

#### 2. 存储元、存储单元和地址分别是什么？

存储元保存一位二进制信息；若干存储元组成可独立编址的**存储单元**；存储单元的唯一编号是**地址**。存储字长是一个存储单元包含的位数，机器字长是 CPU 一次自然处理的位数，指令字长是单条指令的位数。

#### 3. 存储器如何扩展？

- **位扩展**：并联多个芯片，增加每个单元的位数。
- **字扩展**：用地址译码选择芯片，增加单元个数。
- **字位扩展**：两者结合。

#### 4. 什么是 Cache？它为什么有效？

Cache 是 CPU 与主存之间的小容量高速存储器，保存近期可能再次访问的主存块。它利用**时间局部性**和**空间局部性**：命中时直接由 Cache 提供数据，未命中才访问主存，从而降低平均访存时间。

#### 5. Cache 有哪些映射方式？

- **直接映射**：一个主存块只能映射到一个 Cache 行，简单快，但冲突缺失多。
- **全相联映射**：可放任意行，冲突少，但比较器多、代价高。
- **组相联映射**：先按索引定位组，再在组内全相联查找，是常用折中。

#### 6. 写直通和写回如何选择？

**写直通**每次写 Cache 同时写主存，主存始终较新、实现简单，但总线流量大，常配写缓冲。**写回**只写 Cache，脏块替换时再写主存，带宽效率高但需脏位，控制更复杂。写未命中还可选写分配或非写分配。

#### 7. Cache 与虚拟存储器的异同？

二者都基于局部性、都以较小高速层模拟较大低速层。不同在于：Cache 位于 CPU 与主存之间，通常由硬件自动管理，目标是缩短平均访存时间；虚拟存储器位于主存与辅存之间，由软硬件协作，目标是扩展可用地址空间。

#### 8. MMU 和 TLB 的作用？

MMU 负责把虚拟地址转换为物理地址并实施保护。TLB 是页表项的高速缓存：TLB 命中可快速完成地址转换；未命中再查页表，若页不在内存则产生缺页异常。

---

### 三、指令系统与 CPU

#### 1. 一条指令由什么组成？

通常包括**操作码**和**地址码/地址字段**；地址字段还隐含或编码寻址方式。有效地址 EA 是最终操作数所在的地址，立即数和寄存器寻址可不访问主存。

#### 2. 常见寻址方式及用途？

- 直接、间接、寄存器、寄存器间接：在地址与访问次数间取舍。
- **相对寻址**：`EA = PC + 位移`，适合转移指令和位置无关代码。
- **基址寻址**：`EA = 基址寄存器 + 位移`，适合重定位与多道程序。
- **变址寻址**：`EA = 变址寄存器 + 位移`，适合数组和循环。

#### 3. CPU 如何区分指令和数据？

内存中的比特本身没有类型。CPU 由当前阶段和控制信号决定其含义：取指周期把 PC 指向内容送入 IR 并按指令解释；执行周期按指令需要把内容当作操作数读写。

#### 4. 指令周期、机器周期、总线周期和时钟周期？

**时钟周期**是最基本节拍；**机器周期**完成一次基本操作（如访存）；**总线周期**完成一次总线传输；**指令周期**从取一条指令到取下一条指令之间的全部时间，通常包含若干机器周期。不同机器的具体对应关系并不固定。

#### 5. 机器指令和微指令的关系？

机器指令是 ISA 向程序员暴露的指令；微指令是控制器内部发出的微操作控制信号。一条机器指令可由一个微程序的多条微指令实现。硬布线控制速度高但修改困难；微程序控制更易扩展，常与复杂指令集结合。

#### 6. 寄存器 PC、IR、MAR、MDR 分别做什么？

- `PC`：下一条指令地址。
- `IR`：当前正在译码/执行的指令。
- `MAR`：本次主存访问地址。
- `MDR`：本次从主存读出或将写入主存的数据。

#### 7. 指令流水线为什么会阻塞？

三类冒险：**结构冒险**（资源争用）、**数据冒险**（结果尚未产生便被使用）、**控制冒险**（分支去向未知）。缓解方式包括资源复制、转发、暂停/插入气泡、分支预测和投机执行。

#### 8. ALU 与控制器分别负责什么？

ALU 执行算术、逻辑、移位和比较等数据操作；控制器根据指令和状态产生控制信号，组织取指、译码、执行、访存和写回。二者与寄存器、内部总线共同构成 CPU 数据通路。

---

### 四、总线与 I/O

#### 1. 地址线和数据线的作用？

地址线指出被访问的存储单元或 I/O 端口；数据线传送指令、数据和状态信息。地址线宽度决定可寻址范围，数据线宽度影响一次传输的数据量。

#### 2. 同步总线和异步总线？

同步总线依靠统一时钟，控制简单、时序确定，但速度受最慢设备约束；异步总线用请求/应答握手，适合不同速度设备，但控制开销较大。

#### 3. 什么是总线仲裁和总线带宽？

仲裁决定多个主设备同时申请时谁获得总线，可分集中式和分布式。总线带宽是单位时间可传输的数据量，粗略可由“数据线宽度 × 有效传输频率”估计，实际还受协议和等待周期影响。

#### 4. 什么是突发传输？

一次给出起始地址后，连续传输多个相邻数据单元，减少重复寻址和仲裁开销。Cache 填充一个块、DMA 成块搬运都常使用突发传输。

#### 5. DMA 是什么？为什么优先级通常高于 CPU？

DMA 控制器在 CPU 初始化后直接在设备与主存间传送一块数据，仅在开始或结束时中断 CPU。I/O 数据若不能及时搬运可能丢失，因此 DMA 往往有较高总线优先级；它可采用周期挪用、交替访问或突发方式与 CPU 共享主存。

#### 6. 磁盘访问时间由哪些部分构成？

主要为**寻道时间**（磁头移到目标磁道）、**旋转延迟**（目标扇区转到磁头下）和**传输时间**。题目给定具体参数时三者相加即可。

---

### 五、复试速答

#### 1. 内存和 Cache 的区别？

主存通常由 DRAM 构成，容量大、速度较慢；Cache 多由 SRAM 构成，容量小、速度快，自动缓存主存热点数据。两者共同构成层次化存储系统。

#### 2. USB 有哪些优点？

支持即插即用和热插拔，具有统一接口、供电能力、较强扩展性及较高传输速率。它属于外设互连总线，具体速率和拓扑取决于版本。

#### 3. 什么是虚拟机？

虚拟机在一台物理机上模拟多个相互隔离的计算环境，每个环境可独立运行操作系统和应用。它常用于测试、隔离、资源整合和部署；代价是有虚拟化开销。
