# 操作系统

> 回答操作系统问题时，注意区分“硬件异常/中断”“进程/线程”“同步/互斥”等相邻概念。

## 目录

- [一、操作系统概述](#一操作系统概述)
- [二、处理机管理](#二处理机管理)
- [三、内存管理](#三内存管理)
- [四、文件与 I/O 管理](#四文件与-io-管理)
- [常见面试提问](#常见面试提问)
---

## 一、操作系统概述

操作系统管理处理机、内存、文件和设备，并提供用户接口。其核心特征是**并发、共享、虚拟和异步**；它通过进程、虚拟内存、文件和设备抽象管理有限硬件资源。

## 二、处理机管理

进程是资源分配的基本单位，线程是 CPU 调度的基本单位。常见调度算法有 FCFS、SJF、优先级、时间片轮转和多级反馈队列。同步解决先后制约，互斥保护临界资源；PV 操作和管程是典型机制。

## 三、内存管理

连续分配简单但有外部碎片；分页消除外部碎片；分段反映程序逻辑；段页式结合两者。虚拟存储器基于局部性，以请求调页等机制在主存和辅存间换入换出；常见置换为 OPT、FIFO、LRU 和 Clock。

## 四、文件与 I/O 管理

文件由目录和 FCB/inode 等元数据管理，分配方式有连续、链接和索引分配。I/O 可由程序查询、中断、DMA 和通道控制；缓冲、Spooling 和设备独立性用于提高并行性和易用性。

---

## 常见面试提问

#### 一、概述、中断与系统调用

#### 1. 操作系统的功能和特征？

功能包括处理机、内存、文件和设备管理，并提供用户接口。基本特征是**并发、共享、虚拟和异步**：并发与共享互为条件，虚拟把一台物理机的资源抽象为多个逻辑资源，异步使进程推进速度不可预知。

#### 2. 什么是中断？内中断和外中断？

中断使 CPU 暂停当前控制流、转去执行处理程序，结束后恢复。**外中断**来自处理器外部，如 I/O 完成、时钟；**内中断/异常**由当前指令执行引起，如除零、缺页、非法指令。陷入（trap）通常是程序主动执行系统调用指令产生的同步异常。

#### 3. 中断处理的一般过程？

保存现场和程序计数器，按中断向量找到处理程序，执行服务例程，必要时允许更高优先级中断，最后恢复现场并返回断点。硬件负责的最小保存和软件保存的内容依体系结构而定。

#### 4. 用户态、内核态与系统调用？

用户态不能执行特权指令；内核态可访问受保护资源。用户程序经库函数准备参数，执行陷入指令进入内核，由内核校验参数、完成服务，再返回用户态。系统调用是受控的用户—内核接口，并非普通函数调用。

---

### 二、进程、线程与调度

#### 1. 什么是进程？它和程序有什么区别？

进程是程序的一次执行过程，由程序段、数据段和 PCB 构成，是资源分配的基本单位。程序是静态文件，进程是动态实体；同一程序可对应多个进程。

#### 2. 进程的基本状态与转换？

就绪态已具备除 CPU 外的运行条件；运行态正在占用 CPU；阻塞态在等待事件或资源。调度使就绪→运行，时间片耗尽或被抢占使运行→就绪，等待事件使运行→阻塞，事件到达使阻塞→就绪。

#### 3. 并发、并行和互斥？

并发是在同一时间段内交替推进；并行是在同一时刻真正同时执行，通常需多核。互斥是多个执行流不能同时使用同一临界资源。父子进程可并发运行，但共享资源时仍需同步。

#### 4. 线程和进程的关系？多线程一定更快吗？

进程拥有独立资源和地址空间；同一进程的线程共享大部分资源，但各自有 PC、寄存器和栈，是 CPU 调度的基本单位。线程切换通常比进程切换轻，但多线程不必然更快：同步开销、锁竞争、单核限制和任务粒度都会影响性能。

#### 5. 常见调度算法和评价指标？

算法包括 FCFS、短作业优先、优先级、高响应比优先、时间片轮转和多级反馈队列。指标包括 CPU 利用率、吞吐量、周转时间、带权周转时间和响应时间。高优先级进程是否立即运行取决于调度是否可抢占。

#### 6. 三级调度和实时系统？

高级调度决定哪些作业进入内存，中级调度负责挂起/换入，低级调度从就绪队列选进程占 CPU。硬实时系统错过截止期可能造成严重后果；软实时系统允许偶尔错过截止期但服务质量下降。

---

### 三、同步、互斥与死锁

#### 1. 同步和互斥的区别？

同步是有先后约束的直接制约，例如生产者产生后消费者才能取；互斥是对临界资源的间接制约，例如多个进程不能同时修改同一缓冲区。生产者—消费者问题通常同时需要两者。

#### 2. 临界区互斥应满足哪些条件？

**空闲让进、忙则等待、有限等待、让权等待**。前两项保证正确与进展，后两项避免无限等待和忙等浪费 CPU。

#### 3. PV 操作与信号量？

信号量是用于同步和互斥的整数及等待队列。`P(S)` 申请资源：`S--`，若资源不足则阻塞；`V(S)` 释放资源：`S++`，必要时唤醒一个等待者。记录型信号量把阻塞队列与计数结合，避免忙等。

#### 4. 什么是管程？

管程把共享数据、操作共享数据的过程和同步机制封装在一起，任一时刻只允许一个线程在管程内执行。条件变量上的 `wait` 会释放管程锁并阻塞，`signal` 唤醒等待者；它比直接使用信号量更易模块化。

#### 5. 什么是死锁？四个必要条件？

一组进程因互相等待资源而永久无法推进。必要条件是**互斥、不剥夺、请求并保持、循环等待**。处理策略有预防（破坏条件）、避免（如银行家算法保持安全状态）、检测与解除，以及在可接受场景下忽略。

#### 6. 安全性算法与死锁检测算法区别？

安全性算法在分配前或分配时判断系统是否仍存在一个能完成所有进程的安全序列，用于避免死锁；检测算法面对已分配状态，判断当前是否已出现无法推进的进程集合。

---

### 四、内存管理

#### 1. 连续分配和动态分区算法？

连续分配包括单一连续、固定分区和动态分区。动态分区按进程实际大小分配，常用首次适应、循环首次适应、最佳适应和最坏适应；它会产生外部碎片，可通过紧凑或分页缓解。

#### 2. 分页、分段和段页式的区别？

分页按固定大小划分，便于管理且无外部碎片，但不反映程序逻辑；分段按逻辑模块划分，便于共享和保护，但会有外部碎片；段页式先分段再分页，兼顾逻辑视图与页式管理，地址转换更复杂。

#### 3. 重定位与存储保护如何实现？

连续分配可用基址寄存器和限长寄存器：物理地址为逻辑地址加基址，且先检查是否越界。分页/分段依靠页表、段表、MMU 与 TLB 完成地址转换和权限校验。

#### 4. 什么是虚拟存储器？

利用局部性，只把当前需要的部分装入内存，其余保存在辅存，按需调入、暂不用时换出，从而让进程看到比物理内存更大的地址空间。实现方式有请求分页、请求分段和请求段页式；特点是多次性、对换性和虚拟性。

#### 5. 常见页面置换算法？

OPT 理论最优、用于比较；FIFO 简单但可能有 Belady 异常；LRU 利用时间局部性但实现成本高；Clock 用访问位近似 LRU，改进 Clock 还结合修改位。缺页时若有空闲页框可直接调入，并非一定发生置换。

#### 6. 什么是抖动？如何缓解？

进程频繁缺页、调页时间远大于有效执行时间称抖动。常因驻留集太小、并发度过高或置换策略不当；可增加内存、降低多道程序度、采用工作集/缺页率控制等。

---

### 五、文件、磁盘与 I/O

#### 1. FCB 与文件打开过程？

FCB（或 inode）保存文件名、权限、大小、时间和物理位置等元数据。打开文件时系统按路径检索目录，检查权限，在打开文件表建立表项并返回文件描述符；关闭时减少引用并刷新必要数据。

#### 2. 硬链接与软链接？

硬链接是多个目录项指向同一 inode，不能跨文件系统，通常不能链接目录；删除一个名字不影响数据，直到链接计数为零。软链接是独立文件，内容是路径名，可跨文件系统但目标删除后会悬空。

#### 3. 文件分配方式？

连续分配支持顺序和直接访问，速度快但要求连续空间且难扩展；链接分配无外部碎片但通常只支持顺序访问；索引分配支持直接访问且易增长，但要额外索引块。空闲空间可用空闲表、空闲链表、位图等管理。

#### 4. 磁盘调度算法？

FCFS 公平但寻道距离可能大；SSTF 优先最近请求、平均寻道短但可能饥饿；SCAN 像电梯一样单向扫描；C-SCAN 只在一个方向服务，等待时间更均匀。

#### 5. 程序查询、中断、DMA 和通道？

程序查询由 CPU 轮询设备，简单但浪费 CPU；中断由设备完成时通知 CPU；DMA 由控制器成块直接在设备和内存间传输；通道是更专用的 I/O 处理器，可执行通道程序并管理多次传输。

#### 6. 缓冲、Spooling 和设备独立性？

缓冲区用内存协调生产和消费速度、减少中断并支持预读/延迟写。Spooling 把独占设备改造成可排队共享的“假脱机”服务，如打印。设备独立性让用户使用逻辑设备名，由系统映射到具体物理设备。
